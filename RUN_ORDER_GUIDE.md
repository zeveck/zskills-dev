# PR #70 Batch — Execution Order Guide

This is the recommended order for running every plan, queued `/quickfix`, and open issue committed in [PR #70](https://github.com/zeveck/zskills-dev/pull/70) plus issues [#56](https://github.com/zeveck/zskills-dev/issues/56), [#58](https://github.com/zeveck/zskills-dev/issues/58), and [#65](https://github.com/zeveck/zskills-dev/issues/65).

Order matters because several items churn the same files (`skills/update-zskills/SKILL.md`, `dev_server.default_port` schema, the `update-zskills` mirror). Two `/refine-plan` reconciliations are required mid-stream — the rest reduces to staleness gates inside the plans themselves.

---

## Drift log

- **2026-05-11 (continued — `/fix-issues sync` pass; 4 new issues surfaced + #225 scope-update)** — Ran `/fix-issues sync` after #134 closure to verify no other open issues had been silently fixed. zskills has no local issue tracker files (no `*ISSUES*.md` / `ISSUES_PLAN.md` — GitHub issues are the canonical state), so the tracker-update steps are no-ops; the load-bearing work is per-issue research + fixed-verdict checks. **Open issues found: 8** (was 4 in earlier ROG, +4 filed today: #225, #226, #228, #229). Ran careful per-issue audit. **Zero FIXED candidates to close** — all 8 issues correctly remain open. **One partial-fix surfaced:** **Issue [#225](https://github.com/zeveck/zskills-dev/issues/225) Layer 1 is already implemented** at `hooks/block-unsafe-project.sh.template:476` (`is_git_subcommand_in_chain "$COMMAND" commit && is_main_protected && is_on_main` → `block_with_reason`); landed via PR #195 (Plan C, 2026-05-06), refined via PR #197 (chain-wrappers source-of-truth, 2026-05-07). The hook fired live during the audit's grep when the pipeline contained the literal `commit` token — concrete evidence active. The ef3e36f commit that motivated #225 likely happened via `! git commit` in the REPL (terminal channel bypasses PreToolUse hook). Posted scope-update comment ([#issuecomment-4424362086](https://github.com/zeveck/zskills-dev/issues/225#issuecomment-4424362086)) reducing #225's scope to Layer 2 only: `create-worktree.sh` add symmetric `AHEAD_COUNT=$(git rev-list --count origin/main..main)` check after existing `fetch + ff-merge` block. ~10 lines + 2-3 unit cases — `/fix-issues` route, no `/draft-plan` needed.

- **2026-05-11 (continued — #134 wontfix close + plan deletion via PR #230)** — After user pushback on the ROG's suggested next step (`/run-plan docs/plans/ADAPTIVE_CRON_BACKOFF_MODE_B.md`), reviewed what the plan would actually ship: ~30 lines of dead prose in `references/finish-mode.md` describing a failure mode that doesn't exist + cross-reference in `/run-plan` SKILL.md (already 1912 lines vs. ~500-line progressive-disclosure target) + 3 conformance asserts pinning the dead text. **Net diagnosis: the plan was drafted because `/fix-issues plan 134 191 auto` was asked for, not because there was anything to plan.** The Mode B failure mode #134 conceives (cron fires queue up behind paused orchestrator, drain on resume) does not exist in the Anthropic Code harness because the harness is retry-on-next-idle per `finish-mode.md:103,125` (cited fact already drift-anchored by Mode A's existing structure). Closure path: (1) post structured rationale comment on #134 ([issue-comment-4424236296](https://github.com/zeveck/zskills-dev/issues/134#issuecomment-4424236296)) covering harness-behavior citation + load-bearing reasoning (absence-of-trigger, not absence-of-incident) + re-evaluation trigger keyed on harness-change (not missing-incident counting); (2) close #134 via `gh issue close --reason "not planned"`; (3) delete `docs/plans/ADAPTIVE_CRON_BACKOFF_MODE_B.md` (219 lines) via PR [#230](https://github.com/zeveck/zskills-dev/pull/230) (squash `cebb194`, MERGED 2026-05-11T19:37:30Z) — `.landed` marker written. **No skill, hook, script, or test changes.** Process win: caught the "drafting agent doesn't push back on framing" anti-pattern before shipping; the right answer for some issues is "close with a comment, not draft a plan." Memory anchor candidate: when a drafted plan turns out to ship pure documentation describing a non-event, the close-comment IS the deliverable — the plan should be deleted, not run.

- **2026-05-11 (continued — PR #219 rescue + #224 scope-update)** — PR [#219](https://github.com/zeveck/zskills-dev/pull/219) (`do: add requires.land-pr tracker for /run-plan PR mode`, filed by user 2026-05-10T21:39Z) **MERGED via squash `2bdbeda` at 2026-05-11T19:12:16Z** after a multi-step rescue. The PR had been showing CI FAILURE for ~16h, but the FAILURE was a single stale run from before fix commits `c96d9f9` (registry hash for `post-run-invariants.sh`) + `d03d2ce` (version bump + mirror) were pushed; PRs #221/#222/#223 had landed since the branch was based on `ef3e36f`, leaving the PR `DIRTY/CONFLICTING` so CI never re-fired. **Rebase 1:** rebased onto current main, manual conflict-resolve on `metadata.version` frontmatter for `/run-plan` SKILL.md + `/update-zskills` SKILL.md (both had concurrent bumps); recomputed canonical hashes via `scripts/skill-content-hash.sh` (run-plan → `1ec0ef`, update-zskills → `730670`); `frontmatter-set.sh` to set new versions; `mirror-skill.sh` regenerated `.claude/skills/` mirrors; local 2866/2866 PASS; force-push fired CI green `test: pass 1m57s`. **Rebase 2:** PR #227 (SKILL_DESC_TRIM_PLAN draft) landed during the auto-merge wait, putting #219 BEHIND main again; clean 3-commit rebase (no conflicts), force-push, automerge fired squash from earlier `gh pr merge --auto --squash` enablement at 2026-05-11T18:58:50Z (mergeMethod SQUASH preserved across force-pushes). **`.landed` marker written** at `/tmp/zskills-do-requires-land-pr-tracker/.landed` post-merge. **Issue [#224](https://github.com/zeveck/zskills-dev/issues/224) scope-update comment posted** ([#issuecomment-4423411465](https://github.com/zeveck/zskills-dev/issues/224#issuecomment-4423411465)): the user's other agent had filed #224 today without awareness of #219 (filed 2026-05-10), and #224's Pattern 2 strictly subsumes #219's tracker mechanism. Comment classifies #219 as **Pattern 2 narrowly scoped to `/run-plan` ↔ `/land-pr`** (1 of 5 callers) using existing `block-unsafe-project.sh` requires/fulfilled hook, with fulfilled-marker write at Step 8b gated on `.landed` row-6 conjunction (MERGE_REQUESTED ∧ PR_STATE=MERGED ∧ CI_STATUS ∈ {pass,none,skipped}). Remaining #224 scope post-#219: (1) **extend Pattern 2 to the 4 other callers** (`/commit pr`, `/do pr`, `/fix-issues pr`, `/quickfix`) — catches PR #221's in-other-caller bypass shape; (2) **Pattern 1 — tool-boundary block** via new dedicated `hooks/block-bypassed-land-pr.sh` denying `gh pr (create|merge --auto)` at PreToolUse when no `fulfilled.land-pr.*` exists in `.zskills/tracking/$PIPELINE_ID/` — catches PR #218/#223's manual-no-skill bypass shapes; (3) **fulfilled-marker lifecycle reconciliation** — recommend Option B (Step 1 `status: started` for permit-via-existence + Step 9 `status: complete` for forensic record, mirroring `/verify-changes` SKILL.md:239 + :713) over #219's row-6 conjunction; migrate #219's row-6 gate into Step 9's content update; extend `post-run-invariants.sh` invariant #9 to check `status: complete` (not just existence). Effort estimate: ~4-5 phases (was 5-7 in original #224). Decision: land #219 first as working surgical fix, then `/draft-plan plans/BYPASSED_LAND_PR_HARDENING.md` builds on the merged tracker plumbing.

- **2026-05-10 → 2026-05-11 (`/fix-issues 212 215 216 auto` sprint + AC-7 failure cascade + `/fix-issues plan 134 191 auto` plan-drafting + recovery from agent-introduced messes)** — Single multi-hour session producing 4 PRs (1 closed-redundant, 3 merged) + 2 plan drafts + recurring agent-failure patterns worth documenting as lessons.

  **Sprint output (3 PRs landed):**
  - **PR [#221](https://github.com/zeveck/zskills-dev/pull/221)** (squash `45fc622`, MERGED 2026-05-11T13:17Z) — closes #212. Extracts `/run-plan` Phase 4 PR-body progress sync from ~80-line inline-bash prose to `skills/run-plan/scripts/sync-pr-body-progress.sh` (190 lines pure bash, `--pr`/`--plan-file`/`--branch` args, idempotency early-exit, NOTICE/WARNING/ERROR exit codes). 7 unit cases + 9 conformance assertions. Tier-1 registered (hash + script-ownership row + STALE_LIST). Past failure motivating the extract: PR #211's 9-phase run silently froze its PR body at the Phase 1 snapshot for 8 of 9 phase commits because the prose-driven block was orchestrator-skipped with no mechanical signal.
  - **PR [#222](https://github.com/zeveck/zskills-dev/pull/222)** (squash `cd43835`, MERGED 2026-05-11T04:25Z) — closes #215 + #216 (the "AND" in the PR body's "Closes #215 AND #216" prevented GitHub auto-close of #216 specifically; closed manually post-merge). Extracts `/plans rebuild` index-rendering loop to `skills/plans/scripts/render-index.py` (540 lines pure stdlib Python, reads JSON from `python3 -m zskills_monitor.collect`, writes markdown to stdout; canary-precedence is structurally enforced at line 62-64's FIRST branch in `section_for()`). Adds source-staleness auto-rebuild to `/plans` Mode: Show (mtime check; cross-platform `stat -c %Y || stat -f %m`). Removes `/draft-plan` + `/research-and-plan`'s incremental PLAN_INDEX.md mutation. 7 cases (26 sub-assertions) + 5 new conformance asserts. Past failure motivating the extract: manual `/plans rebuild` misclassified 5 canaries with `status: complete` into Complete instead of Canaries (skill spec is "canaries never promote regardless of status"). **Mid-flight AC-7 follow-up commit `00e329e` on the same branch:** AC-7 in `tests/test_plans_rebuild_uses_collect.sh` had previously been fail-loud if `PLAN_INDEX.md` was absent (correct under the old tracked model); post-PR-#218 the file is intentionally regenerable and absent on fresh checkouts, so AC-7 needed to regenerate via the new `render-index.py` helper before checking parity. Verified locally: 20/20 PASS with and without `PLAN_INDEX.md` on disk. This follow-up commit unblocked all 3 in-flight session PRs.
  - **PR [#223](https://github.com/zeveck/zskills-dev/pull/223)** (squash `c408dca`, MERGED 2026-05-11T13:08Z) — docs-only: lands the two plan drafts for #134 + #191 produced by `/fix-issues plan 134 191 auto` + the sprint section appended to `.zskills/audit/SPRINT_REPORT.md` for the /fix-issues 212/215/216 sprint. The two plan files: `docs/plans/ADAPTIVE_CRON_BACKOFF_MODE_B.md` (2 phases, recommends option (c) for #134 — Mode B is "harness-eliminated" because per `finish-mode.md:103,125` the Anthropic cron harness is retry-on-next-idle not queue+drain, so a 5-hour orchestrator pause produces ONE deferred fire on resume, not N queued fires; the trigger condition for the failure mode #134 conceives doesn't exist in this harness) and `docs/plans/RUN_PLAN_FINISH_AUTO_REUSE_SEMANTICS.md` (3 phases for #191 — switch cherry-pick mode in `finish`/`finish auto` to plan-scoped worktree + accumulate-and-land-once-at-end, matching PR mode; explicit single-phase invocations keep per-phase worktrees). **Auto-close failure:** the PR body contained the phrase "closes draft-plan task for #134" — GitHub's lenient auto-close parser matched `closes ... #134` and `closes ... #191` despite the intervening words, prematurely closing both issues even though only the plan DRAFTS landed, not the implementations. Both issues REOPENED manually post-merge with comments naming the parser-leniency cause + linking to the plan files; they stay open until `/run-plan` lands the actual work.
  - **PR [#218](https://github.com/zeveck/zskills-dev/pull/218)** (CLOSED redundant, never merged) — `chore: untrack regenerable caches in .zskills/audit/` (PLAN_INDEX.md + PLAN_REPORT.md). Originally created when user manually attempted a direct `git rm --cached` + `git commit` on main, then discovered main was protected (no `--auto-merge-without-CI` path); converted to PR. CI failed on AC-7 (same root cause that the AC-7 follow-up in PR #222 fixed). The untrack commit was included in PR #222's squash base, so on PR #222's merge, #218's content was already in main; PR #218 was closed-as-redundant with a comment naming the superseding PR.

  **Recurring agent-failure patterns surfaced this session** (lesson-bearing for future runs):
  - **"Pre-existing" framing keeps misfiring** despite memory anchor `feedback_pre_existing_paper_over.md`. Both verifier subagents in the `/fix-issues 212 215 216` sprint classified AC-7 as "pre-existing on main" by reproducing it at `ef3e36f` (local main HEAD with PR #218's untrack commit) — but `ef3e36f` was LOCAL-ONLY; remote main was `c87b447` (no untrack). The 5-step audit per the memory anchor explicitly says to verify against `origin/main`, not whatever HEAD happens to be. Orchestrator caught the framing-error in the sprint report ("verifier judgment failure worth surfacing") and then PROCEEDED AS IF THE VERIFIER VERDICT HELD ANYWAY for purposes of scope-deferral. This is the same shape as `feedback_dont_defer_hole_closure.md`: noticing the hole, naming it, and shipping anyway because closing it felt like out-of-scope work.
  - **Report-and-defer instead of read-and-act.** The AC-7 CI failure was reported in the sprint summary with three "resolution paths" (option A, B, or C) and a "want me to fix it?" question, when the right disposition was just "fix it now." Conversation moved to /draft-plan work; the failing CI sat unaddressed for the rest of the session until user explicitly asked. Pattern: option-presenting deflects to user; doing the obvious thing is what was wanted.
  - **Unnecessary state changes.** After PR #222 auto-merged, orchestrator did `git reset dev/main` (mixed) on local main to "align with new remote main" — a step that wasn't required for any in-flight work (the docs PR was made in its own worktree from `dev/main` directly). The reset created a visually-alarming working-tree state where every PR #222-touched file appeared "modified or deleted" (because the working tree had pre-#222 content vs the new HEAD). User had to ask "why did you make such a mess to fix a test?" before the actual recovery (per-file `git checkout HEAD --`) ran.
  - **Lenient auto-close keyword matching.** PR body language like `closes draft-plan task for #N` matches GitHub's auto-close pattern despite intent prose nearby saying "these should NOT auto-close." Lesson: GitHub's parser doesn't read disclaimer paragraphs; use less-keyword-y phrasing in PR bodies that mention issue numbers without closing them (e.g., "the plan-drafting task for issue 134" rather than "the plan-drafting task for #134"; or explicit "Note: NOT auto-closing #N (work not yet done; reopen if accidentally closed)" markers).
  - **Confidently-wrong factual assertions despite session-long correction.** Multiple instances of citing wrong line numbers, fabricated branch conventions (`pr/<plan-slug>` when actual is `feat/<plan-slug>`), and inverted mode descriptions (claimed PR mode was per-phase when it's plan-scoped; cherry-pick mode IS per-phase). Adversarial review caught each one; the pattern recurred immediately after each correction.

  **New issues filed this session:** **[#220](https://github.com/zeveck/zskills-dev/issues/220)** (filed by user 2026-05-10T21:45Z while watching the AC-7 cascade; CLOSED via PR #222's `00e329e` follow-up commit with comment naming the resolution).

  **Plan + worktree disposition:** docs/plans/ADAPTIVE_CRON_BACKOFF_MODE_B.md + RUN_PLAN_FINISH_AUTO_REUSE_SEMANTICS.md both land on main via PR #223 and are READY FOR `/run-plan` execution. `/tmp/zskills-fix-issue-212` worktree's work landed via PR #221; `/tmp/zskills-fix-issue-215` worktree's work landed via PR #222; `/tmp/zskills-docs-session-plans-and-sprint` worktree's work landed via PR #223. All three could get `.landed` markers + cleanup (none done yet — `/commit land` or manual rm; CLAUDE.md "verify each worktree before remove" applies).

- **2026-05-10 (ZSKILLS_PATH_CONFIG `/run-plan` landed end-to-end + CANARY1 happy + registry-reachability gate)** — Three landings completing the path-config arc and closing a registry-orphan class-of-bug surfaced during the CANARY1 run. **PR [#211](https://github.com/zeveck/zskills-dev/pull/211)** (squash `f0c2a13`, MERGED 2026-05-10T13:18Z) — `/run-plan plans/ZSKILLS_PATH_CONFIG.md finish auto pr` executed end-to-end; **all 9 phases ✅ Done** in a single squash (PR title is misleading — it cites only Phase 1 work, but the diff is 206 files / +7441 / −756 covering Phases 1, 1b, 2a, 2b, 3, 4, 5a, 5b, 6 with per-phase commits `5b9f150`/`6082e1f`/`6c2dc50`/`6b9552f`/`59aeff7`/`99ad5df`/`12042ae`/`0e12b4e`/`9479e09` on the worktree branch `feat/zskills-path-config`). New helper `skills/update-zskills/scripts/zskills-paths.sh` exports `ZSKILLS_PLANS_DIR` / `ZSKILLS_ISSUES_DIR` / `ZSKILLS_AUDIT_DIR`; `config/zskills-config.schema.json` gains `output.{plans_dir, issues_dir}`; `block-unsafe-project.sh` hook regex broadened from `\.zskills/tracking` to `\.zskills` (guards audit + future trees); 21 forbidden-literal patterns lock the conformance gate. Phase 6 self-migration moved 105 files: `plans/` → `docs/plans/` (55), forensic artifacts → `.zskills/audit/` (50), and `var/dev` → `.zskills/dev-server`. New `scripts/migrate-paths.sh` (946 lines) ships as the deterministic migration tool with `--migrate-paths` and `--rewrite-only` flags. CANARY1/6/7 auto-dispatch deferred per orchestrator-subagent-cannot-multi-agent constraint; CANARY8/9/10 manual per runbooks. Plan frontmatter `status: complete; completed: 2026-05-08`. Tests 2747/2747 → 2805/2805. **PR [#213](https://github.com/zeveck/zskills-dev/pull/213)** (squash `e4f8307`, MERGED 2026-05-10T14:16Z) — `/run-plan plans/CANARY1_HAPPY.md` post-migration smoke. Phase 1 refreshed `canary/c1.txt` timestamp to verify `/run-plan` resolves `$ZSKILLS_PLANS_DIR`-rooted plan paths after the migration; tracker → ✅ Done; plan report at `.zskills/audit/plan-canary1-happy.md`; frontmatter flipped `active → complete; completed: 2026-05-10`. **Mid-PR side-fix:** PR #213 CI red on first push; root cause was 4 orphaned `migrate-paths.sh` blob hashes registered in `tier1-shipped-hashes.txt` during PR #211's recovery iteration — only the last hash (`80befeb`) was reachable post-squash-merge, the other 4 became orphans, causing case 2c's `find_blob_for()` to silently produce empty fixtures. Hot-fixed in `cf82c77` by trimming. **PR [#214](https://github.com/zeveck/zskills-dev/pull/214)** (squash `c87b447`, MERGED 2026-05-10T14:58Z) — closes the orphan-registration class-of-bug structurally. Adds Case 6d to `tests/test-update-zskills-migration.sh` asserting every entry in `tier1-shipped-hashes.txt` resolves via `git cat-file -e <hash>` in the local object store. Fires at two key moments: PR-context CI (catches developer-registered unreachable blobs) + post-merge push to main (catches squash-merge orphans within minutes via the existing `main-broken` auto-issue canary). PR #214's gate would have caught PR #211's 4 orphans at PR CI before merge. **Three new issues filed during this stretch** (all routed to `/fix-issues` — design space settled): **[#212](https://github.com/zeveck/zskills-dev/issues/212)** (extract `/run-plan` Phase 4 PR-body progress sync to `sync-pr-body-progress.sh` helper — orchestrator skipped the inline 80-line bash on 8 of 9 phase commits during PR #211; PR body silently stuck at Phase 1 snapshot for the entire 9-phase run); **[#215](https://github.com/zeveck/zskills-dev/issues/215)** (extract `/plans rebuild` index-rendering loop to `render-index.py` helper — agent misclassified 5 canaries with `status: complete` into Complete section instead of Canaries during a manual rebuild); **[#216](https://github.com/zeveck/zskills-dev/issues/216)** (`/draft-plan` + `/research-and-plan` stop touching `PLAN_INDEX.md`; `/plans` Mode: Show auto-rebuilds on source-staleness — architectural fix coupling with #215). All three are the same architectural pattern: prose-driven structured-document editing → mechanical helper. **Worktree state:** `feat/zskills-path-config` (containing per-phase commits 5b9f150–9479e09) is now landed via PR #211 squash; verify `.landed` marker before remove. **`docs/plans/` is now the canonical plans directory** post-migration; legacy `plans/X.md` references in this ROG resolve via `$ZSKILLS_PLANS_DIR` when re-invoked.

- **2026-05-07 (path-config refine + 5-issue cleanup sprint + sprint-finalization)** — Three landings continuing the 2026-05-07 cadence. **PR [#206](https://github.com/zeveck/zskills-dev/pull/206)** (squash `a81607b`, MERGED) — `/refine-plan plans/ZSKILLS_PATH_CONFIG.md 2` adversarial-review refresh absorbing post-2026-05-06 ecosystem drift (Plans A/B/C live, PR #196 cleanup merged, two new helpers `pr-preflight.sh` / `skill-version-compare.sh`). Plan grew 3389 → 3758 lines (+369). Round 1: 62 findings (40 reviewer + 22 DA), 22 fixed + 14 justified-judgment + 4 immutable-section notes. Round 2: 13 new findings, 11 fixed + 1 logged + 3 immutable-section notes; **round 2 caught 13 issues round 1 missed**, validating the 2-round budget over refiner self-claimed convergence at round 1. Highest-impact additions: new Cross-cutting versioning discipline subsection (line 277) closing the `metadata.version`-bump gap before PR #193's PreToolUse hook bites; Phase 6.1 self-migration cron-stall fix (3-step sequence: cancel cron pre-commit → `git mv` → re-issue post-commit); Phase 2a.10 broken `^python3 - <<` AC formula corrected (live: 11 heredocs + 2 `-c` strings, not 6); stale-anchor sweep (`block-unsafe-project.sh.template:201` → `:273`, etc.); Phase 4.0 LD-12 reframed (PR #196 satisfied prerequisite). Byte-preservation verified for immutable sections (lines 1–266 + Plan Quality footer). Drift Log + Plan Review appended per skill spec. **PRs [#207](https://github.com/zeveck/zskills-dev/pull/207) / [#208](https://github.com/zeveck/zskills-dev/pull/208) / [#209](https://github.com/zeveck/zskills-dev/pull/209)** — `/fix-issues 183 188 202 203 205 auto` sprint, 5 issues across 3 PRs (Group A solo #183, Group B bundled #188+#203+#205 to avoid metadata.version conflict, Group C solo #202). PR #207 (`0332696`) closes #183 — `/plans` regex accepts em-dash + en-dash + colon + hyphen separators (was em-dash + hyphen only); +2 assertions in `test_zskills_monitor_collect.sh`. PR #208 (`f8ba2c9`) closes #188 + #203 + #205 — bundled `/land-pr` discipline fixes: always-explicit `git push -u origin "$BRANCH"` + ls-remote post-push verify (#188); `$BASE_BRANCH` → `origin/$BASE_BRANCH` in `.landed` COMMITS_LIST (#203); Step 8 marker auto-detect from cwd via `git rev-parse --show-toplevel` + `git worktree list` membership when `--worktree-path` omitted (#205); +3 cases in `test-land-pr-scripts.sh` + 8 cases in new `test-land-pr-worktree-detect.sh` + 2 conformance asserts. PR #209 (`a4c6ba2`) closes #202 — `tests/test-hooks.sh` `/tmp/inv-test.txt` scoped per-process via `mktemp` + EXIT trap; verified hermetic-under-load during sibling-worktree concurrent verification. **PR [#210](https://github.com/zeveck/zskills-dev/pull/210)** (squash `63e8925`, MERGED) — `docs(sprint): finalize 2026-05-07 11:45 sprint`; SPRINT_REPORT.md `[UNFINALIZED]` → `[FINALIZED 2026-05-07]` for the 8th sprint section. **Sprint metrics:** all 3 PRs CI-green first-try; all 3 verifications used `subagent_type: "verifier"` (Plan A's structural defense exercised across 4 dispatches today including the path-config refine); the path-config refine itself dispatched 5 sub-agents (round-1 reviewer + DA + refiner + round-2 reviewer + DA + refiner) — top-level Skill-tool dispatch per memory `feedback_multi_agent_skills_top_level.md`. **Surfaced follow-up (out of scope):** `tests/test-briefing-parity.sh` shares `/tmp/zskills-briefing-fixture-noport/` between concurrent runs — same class as #202 in a different file; filable as separate fix.

- **2026-05-07 (skill-versioning + run-plan preflight cleanup batch)** — `/fix-issues 177 178 179 194 auto` sprint, 3 PRs landed in sequence on green CI first-try. **PR [#199](https://github.com/zeveck/zskills-dev/pull/199)** (squash `6a0f5b1`, MERGED) closes #177 — adds `skills/run-plan/scripts/pr-preflight.sh` so /run-plan PR-mode preflights that grep `gh pr list` for path-prefix matches can self-filter the pipeline's own PR via `--exclude-pr "${RUN_PLAN_PR_NUMBER:-}"`; SKILL.md prose updated to cite the helper instead of inlining `gh pr list ... | grep -F`; 9-case unit suite. **PR [#200](https://github.com/zeveck/zskills-dev/pull/200)** (squash `704e16a`, MERGED) closes #178 + #179 (bundled — both touch `references/skill-versioning.md`) — adds `scripts/skill-version-compare.sh` (correct same-day-bump comparator: `date >= old AND (date > old OR hash != old)`) + Comparator subsection in `references/skill-versioning.md` + improved `tests/test-skill-conformance.sh` failure message citing `<!-- allow-hardcoded: -->` marker explicitly with placement guidance + formal documentation of `\`\`\`text` fence as non-exec exemption (Appendix C). Audit confirmed zero bare-fence body literals (29 regex matches all in `metadata.version` frontmatter, out of scope). 18-case unit suite. **PR [#201](https://github.com/zeveck/zskills-dev/pull/201)** (squash `e8d637e`, MERGED) closes #194 — `scripts/skill-version-stage-check.sh` STOP message now appends `(SKILL.md not staged — git add it)` when discriminator `on_disk_ver != staged_ver && on_disk_ver != head_ver` fires (case b: bumped on disk but not in staged blob). Variable-name discrepancy from issue body resolved by using actual signal. 2 new test cases. **Sprint metrics:** ~+29 tests added (9+18+2); all 3 verifiers used the new `subagent_type: "verifier"` (first real multi-issue exercise of Plan A's structural defense — Layer 0 timeout-injection + Layer 3 response-validate composed correctly across 3 dispatches); all 3 verifiers correctly applied the 5-step "pre-existing" audit per `feedback_pre_existing_paper_over.md` before classifying the surfaced `test-hooks.sh` flake. **Surfaced follow-up:** Issue [#202](https://github.com/zeveck/zskills-dev/issues/202) — `tests/test-hooks.sh` post-run-invariants block races on unscoped `/tmp/inv-test.txt`; intermittent flake (3 verifiers in this sprint saw 0/1/2 failures of the same test on same main HEAD); root-cause documented; fix is `mktemp` or `$$`-suffix substitution.

- **2026-05-07 (zskills path config — proposal + plan landed)** — PR [#198](https://github.com/zeveck/zskills-dev/pull/198) (squash `af45aa5`, MERGED 2026-05-07). Two docs added under `plans/`: `ZSKILLS_PATH_CONFIG_PROPOSAL.md` (pre-plan design brief) + `ZSKILLS_PATH_CONFIG.md` (3,343-line implementation plan generated via `/draft-plan` 3 rounds adversarial review; converged at round 3 with all P0+P1 addressed). 9 phases: Foundations / mirror-skill+audit / writers excl. /run-plan / /run-plan + CANARY1 / readers + scripts / briefing+dashboard / migration tool deterministic / cross-ref rewrite / self-migration + canary gating + docs. Two-tier curation split: configurable `output.plans_dir` (default `docs/plans`) + `output.issues_dir` (default `.zskills/issues`); fixed `.zskills/audit/` for forensic + regenerated indexes including `PLAN_INDEX.md`. `var/` removed in favor of flat `.zskills/dev-server.{pid,log}`. Helper falls back to legacy `plans/` when config silent — `--migrate-paths` is the explicit opt-in to the new shape. Helper at `.claude/skills/update-zskills/scripts/zskills-paths.sh` (sibling of `zskills-resolve-config.sh`). Conformance test home is `tests/fixtures/forbidden-literals.txt`. Hook regex broadened from `\.zskills/tracking` to `\.zskills`. Cleanup branch (PR #196) was the prerequisite for Phase 4 and merged the same day; mid-amendment refinement re-classified `PLAN_INDEX.md` from Tier 1 to Tier 2 (regenerated index — same shape as `PLAN_REPORT.md` and `VERIFICATION_REPORT.md`). Docs-only PR; no skill, hook, script, or test changes — implementation lands later via `/run-plan` (queued in Phase F below).

- **2026-05-07 (Plan C follow-up — chain wrappers + drift gate 4→7)** — PR [#197](https://github.com/zeveck/zskills-dev/pull/197) (squash `f13b3ab`, MERGED 2026-05-07). F2/F3 follow-up to BLOCK_UNSAFE_HARDENING from `/verify-changes` review. **F2:** the two emergent segment-walking wrappers `is_git_subcommand_in_chain` + `is_destruct_command_in_chain` (added during BLOCK_UNSAFE_HARDENING Phases 3 + 4) were byte-identical between project + generic hooks but not covered by the D7 drift gate; future divergence between the two project/generic copies of `is_git_subcommand_in_chain` would have been undetected. Moved into `hooks/_lib/git-tokenwalk.sh` as canonical source-of-truth. Drift gate `tests/test-hook-helper-drift.sh` extended **4 → 7 byte-identity assertions** (base subcmd / base destruct / chain subcmd / chain destruct across 3 hook consumers). No behavior change — same wrapper bodies, same hooks. **F3:** annotated `reports/plan-block-unsafe-hardening.md` Phase 6 section with post-merge addendum: the `2700` was a worktree measurement; main reports `2701` because `tests/test-skill-conformance.sh` enumerates `.claude/skills/*/` at runtime and main has untracked `.claude/skills/social-seo/` absent from worktrees (environmental, not a regression). Tests 2700→2703 (+3 new drift-gate assertions); drift gate standalone 7/7 PASS. **Plan C cleanly closed.**

- **2026-05-06 (Zimulink viewer URL cleanup)** — PR [#196](https://github.com/zeveck/zskills-dev/pull/196) (squash `6d83778`, MERGED 2026-05-06). Removed Zimulink-specific `viewer/?file=...` URL references from zskills core skills (briefing + fix-report). The URL pattern points at a viewer route part of Zimulink (a separate consumer project), not zskills, and was being emitted/documented unconditionally. Two commits: `85c9c32` (initial briefing/ cleanup) + `88b9a68` (addendum for fix-report/ refs missed in first pass). `tests/test-briefing-parity.sh` 21/21 PASS unchanged. Prerequisite for the path-config refactor on `proposal/path-config` branch.

- **2026-05-06 (Plan C `/run-plan` landed — structural-defense triple complete)** — `BLOCK_UNSAFE_HARDENING` `/run-plan` executed end-to-end via PR-mode chunked `finish auto`, landed via PR [#195](https://github.com/zeveck/zskills-dev/pull/195) (squash `ad22145`). All 6 phases ✅ Done. Tests **2111 → 2700** (+589: 127 unit cases for source-of-truth helpers in P2; 144+192+24 class-pinned matrix cases in P5; integration cases). Generalized `is_git_subcommand` + `is_destruct_command` helpers at `hooks/_lib/git-tokenwalk.sh` source-of-truth (byte-identical inlined into 3 hook consumers via Phase 5.4 drift gate). P3 migrated `block-unsafe-project.sh.template` 6 sites; P4 migrated `block-unsafe-generic.sh` selected sites; P6 consolidated Plan B's `block-stale-skill-version.sh` to consume the source-of-truth (per D6). 8-row D5 carve-out (`rm`/`find`/`rsync`/`xargs`/`fuser-mk` kept-bare) locked by NEGATIVE unit-test cases. Per-phase commits: `2c0c4f1` / `57706e9` / `561a73c` / `d566512` / `e18d1e8` / `38791d3`. **The structural-defense triple (Plans A/B/C — VERIFIER_AGENT_FIX + SKILL_VERSION_PRETOOLUSE_HOOK + BLOCK_UNSAFE_HARDENING) is now complete end-to-end.**

- **2026-05-06 (Plan B `/run-plan` landed)** — `SKILL_VERSION_PRETOOLUSE_HOOK` `/run-plan` executed end-to-end via PR-mode chunked `finish auto`, landed via PR [#193](https://github.com/zeveck/zskills-dev/pull/193) (squash `0e911a6`). All 5 phases ✅ Done across 5 fresh top-level cron-fired turns. Tests 2071 → 2111 (+40 from Phase 2 unit suite + Phase 4 sandbox integration). Closes lock-step gap PR #175 left open: bare `git commit` from terminal now blocked locally by `hooks/block-stale-skill-version.sh` PreToolUse Bash hook (denies on stale `metadata.version` hash; pure-bash JSON escape; tokenize-then-walk `is_git_commit` matcher per Plan B Round 2 N1; documented `bash -c` carve-out). 4 helper scripts (`skill-version-stage-check.sh`, `skill-content-hash.sh`, `frontmatter-{get,set}.sh`) now ship to consumers via new `scripts/install-helpers-into.sh` driver invoked from `/update-zskills` Step D (canonical script-install location post-Plan-B-refinement). CLAUDE.md gains `## Skill versioning` PreToolUse-backstop paragraph documenting verifier-subagent recovery (verifier has Edit+Bash in tool allowlist; SHOULD self-bump from STOP message + retry commit) AND orchestrator-side recovery; cites Anthropic Code docs (https://code.claude.com/docs/en/sub-agents) for hook-composition semantics. **Process notes:** Phase 4 verifier caught + fixed `chmod +x` violation on the new driver script (implementer omitted exec bit); Phase 4 implementer-flagged test-hooks.sh#17 flake was NOT reproducible by verifier across 5 runs (full suite ×2 + isolated ×1 each + sequenced). Phase 3 deferred AC8 (live deny-canary) became Phase 5's `tests/canary-zskills-self-fires.txt` RECIPE doc, backed by Phase 2's 27 unit cases + Phase 4's 13 sandbox cases for automated coverage. Followups: stage-check STOP-message UX nit + recommend `/run-plan plans/BLOCK_UNSAFE_HARDENING.md finish auto` as next step (Plan C).



- **2026-05-06 (Plan A landed + Plan B refined + Plan C drafted)** — Three-step session: (1) Plan A (`VERIFIER_AGENT_FIX`) landed via PR [#189](https://github.com/zeveck/zskills-dev/pull/189) (squash `5db8283`). All 6 phases ✅ Done via chunked `finish auto pr` over 4 hours wall-clock. Closes #176 + #180. **D'' rework absorbed in Phase 1**: dropped L1+L2 (allowlist exclusion + bg-rejection PreToolUse hook + `commit-reviewer.md`) for D'' (Layer 0 `inject-bash-timeout.sh` + Layer 3 `verify-response-validate.sh`). Verifier.md kept full tools allowlist; Layer 0 prevents the bg+Monitor trigger so structural restriction unnecessary. Process win: chunked recurring-`*/1`-cron + Step 0 idempotent re-entry worked end-to-end across 4 fresh top-level turns. **Empirical correction**: Claude Code re-scans `.claude/agents/` on each Agent dispatch attempt — contradicts initial "session-start only" assumption (caveat saved in `feedback_*` memory anchor for future sessions). (2) Plan B (`SKILL_VERSION_PRETOOLUSE_HOOK`) refined via 2 `/refine-plan` rounds: Round 1 (annotation refresh) closed 5 stale anchors after Plan A landing shifted `skills/update-zskills/SKILL.md` Step C; Round 2 (NORMAL scope) closed 8 substantive findings including verifier-hook-composition empirical resolution via Anthropic Code docs (composition CONFIRMED — frontmatter PreToolUse hooks fire ALONGSIDE settings.json hooks, NOT REPLACE), Phase 4.2 Step C → Step D move (canonical script-install home; AC11 inverted), CHANGELOG format mismatch fix to date-only H2 + `### Added —` H3 convention, Phase 3.1c explainer correctly scoped to "Installing 3 PreToolUse Bash safety hooks". (3) Plan C (`BLOCK_UNSAFE_HARDENING`, the follow-up Plan B Phase 5.6 D3 recommended) drafted via `/draft-plan` 2 adversarial rounds; converged at round 2 with 0 critical/high/medium remaining. Generalized `is_git_subcommand` helper at new `hooks/_lib/git-tokenwalk.sh` source-of-truth; covers 6 sites in `block-unsafe-project.sh` + 3 sites in `block-unsafe-generic.sh`; `rm`/`find`/`rsync`/`xargs`/`fuser-mk` kept-bare per D5 carve-out lock to preserve pipeline coverage. **Five in-session reproducers** of the over-match bug class hit during Plan B's refinement + this draft's review confirms the bug is currently active (Plan B round-2 DA grep + sed; orchestrator's `gh issue list --search 'OR'`; prior-art agent's `grep -nE`; round-2 reviewer's heredoc-with-`git checkout --`; refiner's own block during verification). PR3 demoted to UNTRACED (empirically didn't reproduce when isolated — honest docs win over false class-pinned AC). Both Plan B refinement + Plan C draft landed bundled in PR [#192](https://github.com/zeveck/zskills-dev/pull/192) (squash `c68997f`). Independent ship-readiness review on Plan C: SHIP. ROG entries updated for all three.



- **2026-04-27 (early)** — Issue #58 (`main_protected` push-guard regex false-positive) was already closed by [PR #73](https://github.com/zeveck/zskills-dev/pull/73) merged the same day this guide was written. The fix segment-scopes rules (a) and (b) to the `git push` portion of `$COMMAND` (`hooks/block-unsafe-project.sh.template:639-655`) and adds 9 regression tests in `tests/test-hooks.sh`. **Step removed from Phase A**; subsequent steps renumbered.
- **2026-04-27 (mid)** — Issue #56 closed by [PR #74](https://github.com/zeveck/zskills-dev/pull/74) merged. Phase A item 1 marked complete.
- **2026-04-27 (late)** — A drift across 5 PR-creating skills (`/run-plan`, `/commit pr`, `/do pr`, `/fix-issues pr`, `/quickfix`) was surfaced during the `/fix-issues 56 + 58` sprint: each skill duplicates the canonical `gh pr create` + CI poll + fix cycle + auto-merge pattern, with inconsistent gating and one skill (`/quickfix`) opting out entirely. [PR #75](https://github.com/zeveck/zskills-dev/pull/75) (merged 2026-04-27 22:51, commit `82ee65f`) fixes the `/fix-issues` half — interactive PR-mode now runs the full pipeline except the final `gh pr merge`, matching `/run-plan`, `/commit pr`, and `/do pr`. The broader unification was explicitly deferred by PR #75 as out-of-scope ("future `/draft-plan` candidate").
- **2026-04-27 (later)** — `plans/PR_LANDING_UNIFICATION.md` drafted on branch `plans/pr-landing-unification` (worktree `/tmp/zskills-worktrees/pr-landing-unification`). 912 lines, 7 phases (1A foundation → 1B validation → 2 `/run-plan` → 3 `/commit pr`+`/do pr` → 4 `/fix-issues pr` → 5 `/quickfix` → 6 conformance), produced via `/draft-plan rounds 3` + `/refine-plan` YAGNI pass. Creates a new `/land-pr` skill that the 5 callers dispatch via the Skill tool. Branch is based on `47e8344` (pre-PR #75); needs rebase onto main before landing. Phase F entry below moves from "needs draft" to "needs merge".
- **2026-04-28** — Session-of-2026-04-28 PRs merged: PR #76 (no-Haiku CLAUDE.md rule), PR #77 (PR_LANDING_UNIFICATION plan onto main → executable now), PR #78 (QUEUED_QUICKFIXES QF2 + QF4 revised per Opus re-review — when you run those quickfixes, copy from the revised file), PR #79 (QF1 done), PR #80 (`/quickfix` now returns user to base branch on success — fixes a session-friction gap that surfaced during the QF1 run). [Issue #81](https://github.com/zeveck/zskills-dev/issues/81) filed: `main_protected` push-guard rule (c) false-positives on literal `git push` substrings (e.g. inside grep args) — non-blocking but worth picking up alongside the QF2/QF4 runs.
- **2026-04-28 (late)** — Session-end status: Phase A pre-flight is **fully complete**. PR #82 (QF2 — orchestrator-judgment convergence fix), PR #85 (QF4 — `/refine-plan` positional-tail guidance + spaces fix), PR #86 (Issue #83 — `/research-and-plan` Step 3 orchestrator-judgment), PR #87 (Issue #81 — push-guard rule (c) + outer-regex EOL fix), PR #88 (Issue #84 — `scripts/mirror-skill.sh` helper). Three new follow-up issues filed and resolved in-session: Issue #83 (closed by PR #86), Issue #84 (closed by PR #88). Issue #81 closed by PR #87. **Open issues remaining: #65 (block-diagram tracking — addressed by `BLOCK_DIAGRAM_TRACKING_CATCHUP.md` plan in Phase F) and #67 (GitLab support — explicitly deferred).** **/refine-plan triggers added inline** to Phase D and Phase F entries (previously missing from the execution checklist; pre-run hygiene to absorb drift from prior-phase landings).
- **2026-04-28 (later)** — `IMPROVE_STALENESS_DETECTION.md` landed via chunked `finish auto` PR mode: PR #90 (Phase 1: `PLAN-TEXT-DRIFT:` token + `scripts/plan-drift-correct.sh`, 34 cases), PR #91 (Phase 2: `## Phase 3.5` post-implement auto-correct gate, +5 cases), PR #92 (Phase 3: pre-dispatch arithmetic gate sub-checks `a`/`b` + `--eval` token-walking parser with injection canary, +29 cases). Plan frontmatter `status: complete`. 863 → 931 tests passing (+68 plan-drift cases total). The defense-in-depth chain is now complete: `/refine-plan` Dimension 7 (pre-authoring) + Phase 1 step 6 b (pre-dispatch) + Phase 3.5 (post-implement). All 3 layers share the `PLAN-TEXT-DRIFT:` vocabulary and the `plan-drift-correct.sh` helper. Phase A of ROG: every item now `[x]`.
- **2026-04-28 (latest)** — `SCRIPTS_INTO_SKILLS_PLAN.md` (Phase B foundation) landed via chunked `finish auto` PR mode after `/refine-plan` (PR #94 absorbed since-draft drift: `mirror-skill.sh` + `plan-drift-correct.sh` registry entries, hook-blocked `rm -rf .claude/skills/X` recipes replaced with `bash scripts/mirror-skill.sh`, `git rev-parse <commit>:<wildcard-path>` rewrite). Then 6 PRs: #95 (Phase 1: dead-ref cleanup + `script-ownership.md` registry, 14 Tier-1 + 4 Tier-2), #96 (Phase 2: 7 single-owner Tier-1 scripts moved into owning skills), #97 (Phase 3a+3b combined: 7 shared Tier-1 scripts moved + cross-skill caller sweep across 13 skills + hooks + 6 tests + docs + port.sh PROJECT_ROOT bug fix + DEFAULT_PORT_CONFIG Phase 1 inline schema/this-repo-config/template), #98 (Phase 4: `/update-zskills` Step D rewrite + Step D.5 stale-Tier-1 migration via 26-hash known-shipped fixture + CRLF-normalizing hash compare + `command -v git` pre-flight + `port_script` strip; +12 migration tests), #99 (Phase 5: residual sweep + `port_script` schema field removal), #100 (Phase 6: CHANGELOG + RELEASING.md migration note + PLAN_INDEX move + frontmatter flip). Plan frontmatter `status: complete; completed: 2026-04-28`. 931 → 943 tests. **Phase B of ROG complete; DEFAULT_PORT_CONFIG Phase 1 was reconciled inline and is no longer needed as a separate plan**. ROG Phase C's `/refine-plan plans/DEFAULT_PORT_CONFIG.md` step still applies — refine should detect the inline-landed work and strip those WIs.
- **2026-04-29 (later)** — `SKILL_FILE_DRIFT_FIX.md` landed via PR-mode chunked `finish auto`. Refine via PR #121 (2 rounds; 35+36 findings; refine-2 reframed away `__SNAKE__`/sed mechanism in favor of existing model-side `$VAR` substitution discipline at `skills/run-plan/SKILL.md:181`). Then 5 phases on `feat/skill-file-drift-fix` accumulated to PR #122 (squash `f11c67e`): Phase 0 staleness gate (inline preflight), Phase 1 helper script `zskills-resolve-config.sh` + `references/canonical-config-prelude.md` (+24 tests), Phase 2 migration of ~97 hardcoded references across 50 files (+12 tests; INJECTED-BLOCKQUOTE migrated via model-side discipline), Phase 3 hook fallback fix + test-infra detection sync (+9 tests; three-case tree), Phase 4 enforcement: deny-list + drift-warn hook + allowlist convention (+10 tests; 4-entry shared fixture), Phase 5 verification + drift-regression test (+4 tests; positive-side fence-local check surfaced 3 Phase-2 misses, all fixed). Tests 1213→1272 (+59 across plan). Plan frontmatter `status:complete; completed:2026-04-29`. **Phase D of ROG complete**.
- **2026-04-30** — `DEFAULT_PORT_CONFIG.md` landed via PR-mode chunked `finish auto` (PR #125, `da108b4`). Refine via PR #119 (2026-04-28) absorbed Phase B inline-landings (WIs 1.1–1.3) and added Phase P1.A (CHANGELOG correction + greenfield template port_script remnant). Then 5 phases on `feat/default-port-config` accumulated to PR #125: Phase 2 port.sh tightening (regex `[^}]*`→`[^{}]*`, fail-loud guard scoped to main-repo branch, PROJECT_ROOT env override, +3 fixture cases), Phase P1.A CHANGELOG + greenfield template strip, Phase 3 `{{DEFAULT_PORT}}` + `{{MAIN_REPO_PATH}}` substitution mapping (active shipping bug for `{{MAIN_REPO_PATH}}`; conformance test reconciled against SKILL_FILE_DRIFT_FIX assertions; +6 cases), Phase 4 briefing.{py,cjs} path-fix + drop 8080 fallback + omit URL on failure (path drifted when SCRIPTS_INTO_SKILLS relocated port.sh; +5 parity cases), Phase 5 docs sweep (briefing/SKILL.md `<port>` + manual-testing/SKILL.md prose). Tier-1 hash discipline: `tier1-shipped-hashes.txt` updated with new port.sh, briefing.py, briefing.cjs blob hashes. CI green in 54s; auto-merge on green. Tests 1213→1347 (+14 own across the run; +121 from concurrent SKILL_FILE_DRIFT_FIX/DRAFT_TESTS_SKILL_PLAN merges absorbed via inter-phase rebases). Plan frontmatter `status: complete; completed: 2026-04-29`. **Phase C of ROG complete; Phase E now unblocked.** One spec deviation: `tests/test-skill-conformance.sh` reconciled against SKILL_FILE_DRIFT_FIX (#122)'s pre-Phase-3 assumptions (post-reconciliation assertion is stricter, not weaker — verified). Auto-backfill of `default_port` into existing configs explicitly deferred to future-work in CHANGELOG.
- **2026-04-29** — `ZSKILLS_MONITOR_PLAN.md` landed via PR-mode chunked `finish auto`. Refine via PR #101 (2026-04-28; +685/-247 absorbing post-PR-#100 SCRIPTS_INTO_SKILLS drift). Then 9 phase PRs: #102 (Phase 1: `/work-on-plans` execute-only CLI, 943→943 + new skill 677 lines), #104 (Phase 2: retire `/plans work` modes), #107 (Phase 3: queue mutation + scheduling subcommands; SKILL 677→1249, +28 tests), #108 (Phase 4: 1277-line `collect.py` data aggregator; +29 tests), #111 (Phase 5: HTTP server with security contract; +53 tests), #113 (Phase 6: read-only dashboard UI; +45 tests, 1111/1111), #115 (Phase 7: drag-drop + write-back; +47 tests, 1158/1158), #116 (Phase 8: `/zskills-dashboard` skill with cmd+cwd identity check; +35 tests, 1193/1193), #117 (Phase 9: `/plans rebuild` migrated to Python aggregator, no bash fallback; +20 tests, 1213/1213). PR #118 marks plan complete. **Phase F's ZSKILLS_MONITOR_PLAN entry is done; new `/work-on-plans` and `/zskills-dashboard` skills shipped.** Two parallel sessions during this stretch landed `BLOCK_DIAGRAM_TRACKING_CATCHUP.md` (PR #109) and `CONSUMER_STUB_CALLOUTS_PLAN.md` (PR #106).
- **2026-04-29 (sprint)** — `/fix-issues 123 126 93 89 110` sprint via interactive PR mode (5 issues triaged: 4 clear-and-doable, 1 too-complex deferred to `/draft-plan`). Landed in 4 parallel PRs: [#127](https://github.com/zeveck/zskills-dev/pull/127) (issue #123 — `tests/test_plans_rebuild_uses_collect.sh` truncating `$TEST_OUT/.test-results.txt`; renamed RESULTS to per-test scratch path), [#128](https://github.com/zeveck/zskills-dev/pull/128) (issue #126 / QF3 — `/update-zskills` extended source-asset probe + stop-and-ask replacing silent auto-clone; merged 2026-04-30T04:23Z), [#129](https://github.com/zeveck/zskills-dev/pull/129) (issue #93 — hook `extract_cd_target` multi-line bash JSON `\n` decoding; +3 regression tests via real JSON envelopes), [#130](https://github.com/zeveck/zskills-dev/pull/130) (issue #89 — `mirror-skill.sh` orphan-dir test gap; +2 cases, break-and-revert proof). Issue #110 was triaged as too-complex (multi-mode unified design with 6 untested architectural questions) and deferred to `/draft-plan` via `/fix-issues plan` mode. **Notable mid-sprint**: 5/5 sub-agent dispatch crashes (consistent "let me wait for the monitor" hallucination) — root cause unknown after this-session investigation; user observation suggests API-side rate-limit truncation; agent IDs preserved for upstream report. Skill-prose-skip enforcement gap surfaced as [Issue #133](https://github.com/zeveck/zskills-dev/issues/133) — `/commit pr` Step 6 CI poll was skipped during PR #131's landing, which masked the next ROG-relevant landing's CI flake.

- **2026-04-30** — Bookkeeping [PR #131](https://github.com/zeveck/zskills-dev/pull/131) (`docs(plans,sprint): add ADAPTIVE_CRON_BACKOFF plan + 2026-04-29 sprint record`) merged. Captures `plans/ADAPTIVE_CRON_BACKOFF.md` (1199 lines; `/draft-plan` 2 rounds adversarial review; converged at round 2 per orchestrator judgment), the new Ready-to-Run row in `plans/PLAN_INDEX.md`, and the 2026-04-29 fix-issues sprint record + FINALIZED stamps for both 2026-04-27 and 2026-04-29 sprints in `SPRINT_REPORT.md`. CI initially failed on the midnight ET window from [Issue #132](https://github.com/zeveck/zskills-dev/issues/132) (`Intl.DateTimeFormat('en-US', { hour12: false })` defaults to `hourCycle: 'h24'`, emitting `24:HH ET` instead of `00:HH ET` for the midnight hour); rerun outside the window passed; user merged. **Phase F's ADAPTIVE_CRON_BACKOFF entry is now ready to run.**

- **2026-04-30 (later)** — `ADAPTIVE_CRON_BACKOFF.md` (issue #110) executed via PR-mode chunked `finish auto` on `feat/adaptive-cron-backoff`. **Scope: Mode A only** (defer counter at Step 0 Case 3 — solves clean defer pile-up); **Mode B** (failure-fire pile-up where Step 0 is never reached — different machinery, 3 untested bump-check options) explicitly deferred to follow-up [Issue #134](https://github.com/zeveck/zskills-dev/issues/134) per Phase 1 WI 1.0. [PR #138](https://github.com/zeveck/zskills-dev/pull/138) accumulated 4 phase commits: Phase 1 `685d03c` (counter machinery + Step 0 sentinel-recovery prelude with cadence-sanity check + Case 3 decision rule with 3-attempt CronCreate retry + Case 4 + Case 1 + Phase 5b + `/run-plan stop` cleanup), Phase 2 `6131105` (documentation in `references/finish-mode.md` backoff schedule table + 5 reset triggers + DA4/DA5/A1/N2/N4 prose; new step 5 in `references/failure-protocol.md`), Phase 3 `ff24d74` (`tests/test-runplan-defer-backoff.sh` with 14 functional + 2 anchor cases; pure-bash `defer_backoff_step()` extracted from SKILL.md prose mirroring `tests/test-phase-5b-gate.sh:51-92`), Phase 4 `88f414b` (register new test in `tests/run-all.sh` line 52; extend `tests/test-skill-invariants.sh` with 4 #110 anchors → 36→40 pass). Plan frontmatter `status: complete` (`6cd561e`); issue #110 closed via Phase 5b's `gh issue close` with summary comment + commit hashes. PR #138 awaiting merge as of session end. One self-race observed during Phase 4: recurring `*/1` cron fired while phase work was in progress because the tracker wasn't marked 🟡 In Progress before work began (the new Mode A backoff machinery would have handled this if marked); cron deleted explicitly to stop spurious fires. **Phase F ADAPTIVE_CRON_BACKOFF entry done modulo PR #138 merge.**

- **2026-05-01** — [PR #138](https://github.com/zeveck/zskills-dev/pull/138) (`ADAPTIVE_CRON_BACKOFF.md`, issue #110) merged 2026-04-30T22:06 UTC, merge commit `3a49a36`. Phase F's two ADAPTIVE_CRON_BACKOFF entries flipped `[~]` → `[x]`. Mode A defer-pile-up backoff is now live; Mode B (failure-fire pile-up) follow-up is open as Issue #134 — see Open issues subsection below for routing.

- **2026-04-30 (later)** — `/fix-issues 132 133 auto` sprint (PR mode resolved from `execution.landing: "pr"` config). Both issues clear-and-doable per the Open Issues subsection. 2 parallel agents in worktrees on `fix/issue-NNN`; #132 agent crashed mid-task with the recurring "let me wait for the monitor" hallucination (now 6/7 dispatches across two sessions), but had already completed all the worktree work — orchestrator re-ran tests, sanity-reviewed diff, committed. #133 agent reported back cleanly. Both PRs landed: [#141](https://github.com/zeveck/zskills-dev/pull/141) (issue #132, merge `43a2071`; required a fix-up commit because `hour12: false` + `hourCycle: 'h23'` is non-portable per MDN — older Node/ICU silently ignores hourCycle when hour12 is set; fix-up dropped `hour12: false` and added a `parts.hour === '24'` → `'00'` belt-and-suspenders), [#142](https://github.com/zeveck/zskills-dev/pull/142) (issue #133, merge `b77d589`; CI pass first try; rebased after #141 merge moved main forward). #133 fix declined option 3 (tracking marker + hook block) per issue body's own recommendation in favor of option 1 (past-failure prose preamble) + option 2 (script extraction to `poll-ci.sh`) — declared option 3 "overkill for a polling step" and demonstrated mechanical enforcement via "agent must invoke a named script" without cross-skill hook complexity. Phase F's Open Issues entries for #132 and #133 flipped `[ ]` → `[x]`. **Pre-existing failure** observed on both worktrees and main: `tests/test-update-zskills-migration.sh` case 6c (`detect-language.sh` cohabitation, originated in PR #140 DRAFT_TESTS Phase 6, `522cc9e` — registered in script-ownership but missing from `tier1-shipped-hashes.txt`). Out of scope for this sprint; should be filed as a separate issue or rolled into a follow-up.

- **2026-05-01 (recovery)** — The "pre-existing failure" referenced in the prior drift log entry turned out to be **NOT pre-existing** (orchestrator-discipline failure in the framing). Investigation revealed: `tests/test-update-zskills-migration.sh` case 6c contained a `pass "shallow clone — skipped (warning)"` short-circuit that silently treated the test as PASSING under shallow clones (default `actions/checkout@v4` `fetch-depth: 1`). This made case 6c invisible in CI for ~24h while drift accumulated on `main` across PRs #128, #131, #135-#142. Branch protection only enforces the `test` status check on PR refs (`refs/pull/N/merge`) — not on `main` HEAD after merge — so post-merge red on main was completely unsignaled. Four-phase recovery landed end-to-end: [PR #145](https://github.com/zeveck/zskills-dev/pull/145) (Phase A, `509afae`) restored main green by backfilling 10 drifted Tier-1 blob hashes (apply-preset.sh, briefing.cjs, compute-cron-fire.sh, detect-language.sh, land-phase.sh, plan-drift-correct.sh, post-run-invariants.sh, sanitize-pipeline-id.sh, worktree-add-safe.sh, write-landed.sh). [PR #147](https://github.com/zeveck/zskills-dev/pull/147) (Phase B+E, `deb955f`) replaced case 6c's skip-as-pass with hard `fail` + real `skip` accumulator, set CI `actions/checkout@v4` to `fetch-depth: 0`, AND added a stricter unconditional drift invariant in `tests/test-skill-invariants.sh` (40→41 PASS). [PR #148](https://github.com/zeveck/zskills-dev/pull/148) (Phase C, `2107db3`) added explicit `Bash`-tool-`timeout: 600000` guidance and an anti-pattern callout against `Monitor`/`BashOutput` retry to 4 skills (`run-plan`, `fix-issues`, `verify-changes`, `do`) — root cause of the 6+ subagent crashes was that the test suite (~230s) exceeded the default Bash 120s timeout, agents retried with `Monitor`, and the wake events don't reliably deliver to one-shot subagents. [PR #149](https://github.com/zeveck/zskills-dev/pull/149) (Phase D, `bf6dd32`) added an auto-issue canary: post-merge red Tests on main now files (or comments on) a `main-broken` labeled issue. Total: 4 PRs, 1697→1698 tests, all 4 post-merge CI runs on main green, ~24h-old gap closed. Memory anchor `feedback_pre_existing_paper_over.md` (TBD) recommended to the user.

- **2026-04-30 (issues filed)** — Three follow-up issues filed during this session: [#132](https://github.com/zeveck/zskills-dev/issues/132) (`tests/test-briefing-parity.sh` midnight ET `Intl.DateTimeFormat` h24 quirk in `briefing.cjs` — one-line fix specced via `hourCycle: 'h23'`; regression test plan included; affects ~4% of CI runs landing in 00:xx ET window), [#133](https://github.com/zeveck/zskills-dev/issues/133) (`/commit pr` Step 6 CI poll skipped without enforcement — proposed mechanical fix via tracking marker + hook block; light-touch fixes (past-failure prose, script extraction) deemed insufficient per user feedback during PR #131's ROG-discovered miss), [#134](https://github.com/zeveck/zskills-dev/issues/134) (Mode B failure-fire pile-up backoff, follow-up to #110, filed by Phase 1 WI 1.0 of `ADAPTIVE_CRON_BACKOFF.md`).

- **2026-05-01 (later)** — Side fixes from first-ever `/zskills-dashboard start` on this checkout. **PR [#167](https://github.com/zeveck/zskills-dev/pull/167)** (`d957eae`): track `.playwright/cli.config.json` (the file `.devcontainer/setup.sh` writes with `--no-sandbox` for Docker) + add `.playwright/*` to `.gitignore` with un-ignore for the config — eliminates the silent-Playwright-broken state on a checkout where postCreate didn't reach the config-write step. **PR [#168](https://github.com/zeveck/zskills-dev/pull/168)** (`b58d40a`): `.modal-root` was permanently rendered because `display: flex` (line 265 of `app.css`) overrode the browser-default `[hidden] { display: none }` rule (equal CSS specificity, lost on source order); on every fresh page load, users saw a blank modal-card with × button over a 70%-opacity backdrop with no dismiss path. Fix: add `.modal-root[hidden] { display: none; }`. Audit confirmed only one element with this bug pattern. Bundled the one-time `.claude/zskills-config.json` absorption (`dashboard.work_on_plans_trigger: ""` block written by `ensure_dashboard_config_block()` on first dashboard start) — root-cause filed as #165. Two new issues filed: [#150](https://github.com/zeveck/zskills-dev/issues/150) (`tests/test_zskills_monitor_collect.sh` worktree-portable case skips silently in CI, sister of case 6c) and [#165](https://github.com/zeveck/zskills-dev/issues/165) (dashboard config migration belongs in `/update-zskills` + schema, not server-side). Both routed to `/fix-issues` in Open Issues subsection.

- **2026-05-01 (later)** — `PR_LANDING_UNIFICATION.md` landed end-to-end via PR-mode chunked `finish auto` in a single autonomous session (7 PRs total). Phase 1A foundation (PR #159, `1c6c143`): new `/land-pr` skill (552-line SKILL.md + 4 scripts: `pr-rebase.sh`, `pr-push-and-create.sh`, `pr-monitor.sh`, `pr-merge.sh` + 2 caller-facing references: `caller-loop-pattern.md`, `fix-cycle-agent-prompt-template.md`); smoke checkpoint exercised real PR #158 with `--no-monitor`; one foundation bug surfaced + fixed pre-emptively (branch names with `/` broke `/tmp` sidecar paths → `BRANCH_SLUG="${BRANCH//\//-}"` for filenames only). Phase 1B validation (PR #160, `8d9bc13`): `failure-modes.md` (10-mode catalog) + `tests/mocks/mock-{gh,git}.sh` (fail-fast subcommand router) + `tests/test-land-pr-scripts.sh` (23 unit cases) + 37 conformance assertions + 5 new helpers (`check_not`, `check_in_file`, `check_not_in_file`, `check_executable`, `check_not_in_file_filtered`). Phase 2 `/run-plan` migration (PR #161, `e28c7f6`): inline rebase+push+create+CI poll+fix-cycle+auto-merge block in `skills/run-plan/modes/pr.md` (681→545 lines) replaced with `/land-pr` Skill dispatch + canonical caller-loop; body splice (BASH_REMATCH at SKILL.md:1715-1745) preserved as caller-owned; agent-assisted rebase resolution + fix-cycle agent dispatch preserved. Phase 3 `/commit pr` + `/do pr` migration (PR #162, `bc36e6a`, drift fix): both gain canonical fix-cycle (previously report-only); `skills/commit/scripts/poll-ci.sh` deleted as orphan (canonical successor is `pr-monitor.sh`); `/do/SKILL.md` "PR mode CI is report-only" Key-Rule REMOVED (factually wrong post-migration); literal `gh pr create` dropped from `/do/SKILL.md:878`; PR #131 past-failure preamble relocated to `/land-pr` SKILL.md prose. Phase 4 `/fix-issues pr` migration (PR #163, `a14c441`): per-issue caller-loop dispatching `/land-pr` inside the `for issue in FIXED_ISSUES` loop; `--issue=$ISSUE_NUM` passthrough populates `.landed`'s issue field; 300s timeout special case dropped (default 600s applies); /fix-issues pr GAINS canonical fix-cycle (drift fix). Phase 5 `/quickfix` migration (PR #164, `4176427`, drift fix): Phase 7 inline PR creation block replaced with caller-loop; "Fire-and-forget" prose REMOVED (line 20 + 1102) → "triage→review→commit→push→PR→CI poll→fix cycle"; pre-PR triage + plan-review gates (post-#151) preserved; fulfillment-marker model preserved (no `--worktree-path`, no `.landed`); 2 dual-artifact systems coexist intentionally; `tests/test-quickfix.sh` adjusted (Case 13 regex tightened — strengthened, catches `printf > .landed` writes the old regex missed; Case 43 trimmed + 43b structural assertions added; `extract_full_flow` stops at Phase 7 since it now dispatches Skill tool). Phase 6 drift-prevention + canary (PR #166, `e8b6069`, FINAL): 8 cross-skill anchored-pattern conformance tripwires in `tests/test-skill-conformance.sh` (no `gh pr create` / `gh pr checks --watch` / `gh pr merge` outside `skills/land-pr/`; 5 callers dispatch /land-pr; orchestrator-level dispatch heuristic — no nested Agent block ±15 lines around dispatch); `plans/CANARY_LAND_PR.md` (276 lines, deliberate-fail iteration 1 → fix-cycle → iteration 2 success); 27-line drift-prevention rationale citing the 4 motivating drift bugs (87af82a, 1de3049, 175e4aa, b904cef); `plans/PLAN_INDEX.md` → Complete; CHANGELOG 2026-05-01 entry; frontmatter `status: complete` folded in (matches DRAFT_TESTS / ADAPTIVE_CRON_BACKOFF / IMPROVE_STALENESS_DETECTION pattern). Tests 1722 → 1801 (+79 net). All 5 PR-landing call sites (`/run-plan`, `/commit pr`, `/do pr`, `/fix-issues pr`, `/quickfix`) now dispatch `/land-pr`; 4 of 5 callers GAINED canonical fix-cycle (drift fix). **Notable canary observations**: Phase 2 was the bootstrap canary (Phase 3 used the new `/run-plan` code; Phase 3+ used `/land-pr` dispatch via Skill tool — verified-in-action across 5 successive `/land-pr` invocations, all auto-merged cleanly). One CI flake observed during Phase 3 land step: `tests/test-briefing-parity.sh:port-failure` minute-boundary timing race (sister to issue #132 midnight ET) — single rerun passed; non-blocking. One transient test in Phase 6 worktree (`test_plans_rebuild_uses_collect.sh AC-7`) due to worktree-vs-main-checkout race for the new `CANARY_LAND_PR.md` file in PLAN_INDEX; resolves on merge per CI's `actions/checkout@v4 fetch-depth: 0` semantics — verified prediction correct (PR #166 CI passed first try). **Manual-canary WIs deferred** (2.9, 3.5/3.10, 4.8, 5.8, 6.6) per architectural constraint — multi-agent skills (e.g., `/run-plan` against `CANARY_LAND_PR`) cannot run from subagent context (`feedback_multi_agent_skills_top_level`); the 5 successive end-to-end `/land-pr` dispatches + Phase 6's CANARY plan serve as de-facto canaries. **Phase F's PR_LANDING_UNIFICATION entry done; SKILL_VERSIONING now unblocked** (was waiting on `/land-pr` extraction to settle `skills/commit/SKILL.md` Phase 5 collision).

- **2026-05-01 → 2026-05-02** — `/fix-issues 143 150 165 auto` parallel batch executed (3 issues, 3 worktrees, 3 fix agents + 3 verification agents, all dispatched in 2 single-message Agent calls). All 3 PRs merged auto on green CI first try: [PR #169](https://github.com/zeveck/zskills-dev/pull/169) (`a71a68d`, issue #143 — Agent-tool preflight on 5 multi-agent skills), [PR #170](https://github.com/zeveck/zskills-dev/pull/170) (`4c92fbd`, issue #150 — worktree-portable case stops skip-as-pass in CI via temp-worktree materialization), [PR #171](https://github.com/zeveck/zskills-dev/pull/171) (`12a405b`, issue #165 — dashboard config migration moved to `/update-zskills` + schema; `ensure_dashboard_config_block` removed from server.py). Bookkeeping commit [PR #172](https://github.com/zeveck/zskills-dev/pull/172) (`87151c7`) appended sprint section to `SPRINT_REPORT.md`. Tests 1801→1807. **Concurrent-suite-execution flakes** observed in `test-briefing-parity.sh "parity: worktrees"` (live `git worktree list` race between parallel verifiers) and `tests/test-hooks.sh "post-run-invariants.sh"` (shared `/tmp/inv-test.txt` between cases) — confirmed flakes via isolated re-runs (365/365 PASS); won't manifest in CI (one container per PR). Worth filing as a separate test-isolation issue if recurring. **Two non-blocking follow-up items flagged in sprint report**: (a) `/land-pr` step 8 commits-list uses local `main` ref instead of `origin/main` so `.landed` markers in sequential-PR sessions include preceding-PR merge SHAs as cosmetic noise; (b) test-isolation issues per above. Open Issues subsection updated: #143/#150/#165 flipped `[ ]` → `[x]`. **Open issues remaining**: #134 (Mode B failure-fire pile-up — `/draft-plan` first), #67 (GitLab — Phase G).

- **2026-05-02** — `/refine-plan plans/SKILL_VERSIONING.md` 2-round refine completed in worktree `/tmp/zskills-refine-skill-versioning` on branch `refine/skill-versioning`. Round 1: 38 findings (21 reviewer + 17 DA), 28 fixed + 9 justified + 1 partial. Round 2 caught 1 regression Round 1 introduced (the `json-set-string-field.sh` awk rewrite produced invalid JSON — dropped opening quote on update path AND trailing comma on insert path); fixed in Round 2. Round 2 totals: 17 findings (11 reviewer + 6 DA), 14 fixed + 3 justified. Plan grew 1339→1815 lines. Cumulative across all 5 refine + draft rounds (3 /draft-plan + 2 /refine-plan): 93 findings dispositioned, 80 fixed, 13 justified. Pre-review of /refine-plan invocation prompt by independent agent caught 2 rubber-stamp risks pre-issue (per `feedback_draftplan_prompt_detail`); both addressed before launch.

- **2026-05-02 (run + 2 follow-ups)** — `SKILL_VERSIONING.md` `/run-plan` executed end-to-end via PR-mode chunked `finish auto`, landed via [PR #175](https://github.com/zeveck/zskills-dev/pull/175) (squash `f0ef441`). 7 phase commits. Date+hash hybrid versioning shipped (per-skill `metadata.version: "YYYY.MM.DD+HHHHHH"`); 3-point enforcement chain wired (edit-time warn + commit-time stop + CI gate); helper scripts added (`frontmatter-{get,set}.sh`, `skill-content-hash.sh`); `/update-zskills` UI extended. **Two enforcement gaps surfaced during the run** were filed as issues + drafted as plans in the same session: (1) **Issue [#176](https://github.com/zeveck/zskills-dev/issues/176)** (Monitor anti-pattern recurrence) — every Phase-1-through-Phase-7 verifier dispatch reflexively reached for `Bash(run_in_background:true)` + `Monitor`/`BashOutput` polling on the 230s test suite (default 120s Bash timeout exceeded; wake events don't reliably deliver to one-shot subagent dispatches; PR #148's prose warning failed mechanically); orchestrator did inline verification across 5 of 7 phases. (2) **Issue [#180](https://github.com/zeveck/zskills-dev/issues/180)** (verifier-skipped silent pass) — orchestrator logs a one-line note and proceeds instead of invoking Failure Protocol. Both motivated **Plan A: VERIFIER_AGENT_FIX** (queued in Phase F).

- **2026-05-02 (Plan A + Plan B drafted in parallel)** — Two structural-defense plans drafted via `/draft-plan` to close enforcement gaps PR #175 surfaced. Both worktree-isolated, prompt-reviewed before draft, plan-reviewed after draft, committed + automerged.

  **Plan A — VERIFIER_AGENT_FIX** ([PR #181](https://github.com/zeveck/zskills-dev/pull/181), squash `144c85d`, MERGED 2026-05-02). Closes #176 + #180 when `/run-plan`-executed. Three-layer structural fix: tools-allowlist subagent at `.claude/agents/verifier.md` (excludes Monitor/BashOutput/Agent) + frontmatter PreToolUse hook on Bash rejecting `run_in_background: true` (key-boundary-anchored regex) + `/run-plan` Phase 3 Failure Protocol clause that STOPs on stalled-string match (last-N-lines anchor, 7 behavioral phrases) or sub-200-byte response. Separate `commit-reviewer.md` agent for `/commit`'s read-only phase (word-boundary regex catches `&&`/env-prefix/subshell/pipe bypasses). 5 dispatch sites migrated to explicit `subagent_type:` parameters. `/draft-plan` 2 adversarial rounds: Round 1 = 17 findings closed; Round 2 = 9-of-10 closed (N9 deferred via filed-issue work item). Independent ship-readiness review: SHIP. **Notable Round-2 verified defects** (pattern: Round 1 fixes look correct but fail under empirical testing; Round 2 catches via `claude -p` / `claude --help` re-runs): `/agents reload` is FICTIONAL in Claude Code 2.1.126 (`claude -p '/agents reload'` returns "isn't available"); `--agents` CLI flag takes inline JSON not file paths (Phase 1.1 canary spec rewritten as canonical two-script flow with `/agents reload` references replaced by restart-only recovery). Plan grew 660→827 lines.

  **Plan B — SKILL_VERSION_PRETOOLUSE_HOOK** ([PR #182](https://github.com/zeveck/zskills-dev/pull/182), open with automerge SQUASH, BEHIND on main). Closes the lock-step gap PR #175 left open: bare `git commit` from terminal bypasses `/commit` step 2.5's stage-check. Fix is a PreToolUse Bash hook (`hooks/block-stale-skill-version.sh`) that intercepts `git commit` invocations and DENIES on stale `metadata.version` hashes. PreToolUse fires before git executes, so `--no-verify` is irrelevant by construction. Decisions D1-D5: flat hook (no `.template`; cosmetic per research F4); commit-only gating (push gating dropped — cached-diff is empty at push time); KEEP /commit step 2.5 (defense-in-depth); pure-bash JSON escape; mandatory consumer helper-script install (research F1 verified zero copy lines for the 4 stage-check helpers in current `/update-zskills`). `/draft-plan` 2 adversarial rounds: Round 1 = 17 closed (D6 collapses into R1); Round 2 = 10 closed. Independent ship-readiness review: SHIP. **Notable Round-2 verified defects** (same pattern as Plan A — Round 1 fixes verified empirically broken in Round 2): regex still bypassed `git --no-pager commit`/`--git-dir=`/`-P`/etc. — Round 2 pivoted to **tokenize-then-walk** `is_git_commit` shell function (more robust than any regex); bash range strip pattern `[$'\x00'-$'\x1f']` empirically only matched 0x1F not the full range — replaced with POSIX `[[:cntrl:]]`; Phase 4.2 cited wrong Step (Step B is rules-render; Step C is hook/script-copy) — corrected. Plan grew 430→634 lines.

  **Ordering for the two `/run-plan` runs (both queued, neither executed yet):** Plan A FIRST (Plan A's verifier subagent + Failure Protocol clause should be live before Plan B's run, so Plan B's verifier dispatches benefit from the structural Monitor anti-pattern defense). Both plans land as single PRs with all phases as ordered commits (no mid-plan merge); Plan B's Phase 5 AC8 enforces same-PR invariant for the Phase 3 hook + Phase 4 helpers atomicity (no rollout window where consumers have hook but no stage-check script).

  **Process pattern observation:** both plans had Round 1 fixes that LOOKED correct but failed empirical verification in Round 2. The pattern is "refiner accepts a plausible-sounding regex/escape/citation that hasn't been re-tested under adversarial pressure." Round-2 budget is load-bearing — running rounds=3 budget produces materially better plans than convergence-after-1. This validates the user's `feedback_convergence_orchestrator_judgment` rule (convergence is orchestrator's call, not refiner's self-declaration).

- **2026-05-02 (Plan B Phase 5.6 redirect)** — User pushback on Plan A/B follow-up framing surfaced a real pattern: the 3 deferrals from Plan A/B were 1 partial-punt (N9 — whitelist finite recall hardening) + 2 principled deferrals (block-unsafe-project.sh:404 over-match; skill-version-stage-check.sh STOP message). User then noted the block-unsafe-project pattern: this hook has been patched twice already (PR #73 closing Issue #58 segment-scoped push-guard; PR #87 closing Issue #81 push-guard rule (c) + EOL fix), and DA's reproducer in Plan B's Round 1 surfaced a third over-match (line 404 bare `git[[:space:]]+commit` lacks boundary anchoring + data-region redaction; trips on heredoc bodies and grep regex args). **Filing another `404`-specific issue would queue a third patch in a pile rather than fix the regex-fragility class** — same root cause as Plan B's own Round-1→Round-2 evolution (regex extension → tokenize-then-walk pivot in `is_git_commit` shell function). [PR #184](https://github.com/zeveck/zskills-dev/pull/184) (squash `53e963a`, MERGED 2026-05-02T22:43Z) edits Plan B's already-merged `plans/SKILL_VERSION_PRETOOLUSE_HOOK.md` work item 5.6 + AC7 to redirect: block-unsafe-project follow-up becomes a `/draft-plan plans/BLOCK_UNSAFE_HARDENING.md` recommendation in PR body + CHANGELOG (no `404`-specific issue filed; AC7 negative assertion locks the routing decision). Skill-version-stage-check STOP message stays as an issue (it IS a one-line `[ -z "$staged_ver_was_set_initially" ] && hint=...` UX nit, not architectural). New Phase G backlog row added for `BLOCK_UNSAFE_HARDENING.md`. Process lesson: when a hook has been patched 2+ times for the same class of bug, the next finding should pivot the route (issue → plan), not stack another patch. Pattern check: if you find yourself filing an issue for a hook that already has 2+ closed-by-PR-N entries in its history, draft a hardening plan instead.

- **2026-05-02 (agent process failure: automerge state misreporting)** — While shipping PRs #182 and #184, agent (this session) repeatedly reported `mergeStateStatus: BLOCKED` from a query taken seconds after `gh pr merge --auto --squash` enablement, then walked away with framing like "BLOCKED on CI per expected — will fire green." User caught that PR #184 had already merged (squash `53e963a`) at the time of the report. Both PRs eventually merged through luck, not principled monitoring. Root cause: the immediate `gh pr view --json mergeStateStatus` query captures the state the millisecond after enablement (CI not started, branch potentially behind), conflates BEHIND/UNSTABLE/blocked-on-checks-pending into a single "CI pending" framing, and never re-queries. Filed [Issue #185](https://github.com/zeveck/zskills-dev/issues/185). After 4 framing iterations (memory→CLAUDE_TEMPLATE.md→/commit-pr-only→/land-pr-via-Skill-tool), settled on the right propagation surface: CLAUDE_TEMPLATE.md + zskills CLAUDE.md rule mandating `/land-pr` dispatch (or one of its 5 callers), plus `skills/land-pr/SKILL.md` line 4 wording fix so agents stop misreading "not designed for direct user invocation" as "agents can't dispatch it" (the supported path IS agent-via-Skill-tool — the restriction is only against interactive human slash-command typing). Memory anchor `feedback_automerge_blocked_means_act.md` saved as supplementary (agent-local). User pushed back on the memory-anchor-as-fix framing; filed sibling [Issue #186](https://github.com/zeveck/zskills-dev/issues/186) capturing the meta-rule that memory anchors don't propagate. Both #185 + #186 closed via [PR #187](https://github.com/zeveck/zskills-dev/pull/187) (squash `cf8e44e`, bundled — same files): rules added to both CLAUDE_TEMPLATE.md + CLAUDE.md, /land-pr SKILL.md line 4 rewritten + `metadata.version` bumped, 9th conformance tripwire added (4 PASS lines locking the rule literals). One pre-merge process failure surfaced + recovered: `pr-push-and-create.sh` exit 13 on first attempt (push silently lost a race with the gh-credential warnings polluting stderr/stdout); manual push succeeded; /land-pr re-run per its idempotency contract completed cleanly. **Process meta-lesson** (compounded across the session): the canonical path skills (`/commit pr`, `/quickfix`, `/fix-issues`, dispatching `/land-pr`) feel heavyweight for small one-off edits, but the heaviness IS the discipline that catches process failures (triage, plan review, proper CI monitoring, idempotent re-invocation). Bypassing them via direct `gh pr create`/`gh pr merge --auto` invites snapshot-state misreporting + lost-race silent failures.

- **2026-04-30** — `DRAFT_TESTS_SKILL_PLAN.md` landed end-to-end via PR-mode chunked `finish auto` (6 PRs over a single session). Refine via PR #120 (2026-04-29) absorbed post-2026-04-24 ecosystem changes. Then 6 phase PRs: #124 (Phase 1: skeleton + `parse-plan.sh` parser with fenced-code-block-aware checksum gate + 3-predicate AC-ID classifier + 7 fixtures, +62 tests, `2cf6897`), #135 (Phase 2: `detect-language.sh` + `insert-prerequisites.sh` for no-test-setup `## Prerequisites` insertion + 9 fixtures, +53 tests, `e7b4d66`; absorbed PR #122 SKILL_FILE_DRIFT_FIX mid-run via two trivial conflicts), #136 (Phase 3: `append-tests-section.sh` + `draft-orchestrator.sh` with parsed-state-driven single-source-of-truth delegate/ac-less skip + 4 fixtures, +64 tests, `b1b8906`), #137 (Phase 4: `review-loop.sh` + `coverage-floor-precheck.sh` + `convergence-check.sh` adversarial QE-persona loop with **orchestrator-judgment convergence** — refiner self-call ignored, mechanical 4-condition disposition-table check; merged-candidate coverage-floor pre-check; exit codes 0/2/3/6; +10 fixtures, +79 tests, `c9ebf31`), #139 (Phase 5: `gap-detect.sh` + `append-backfill-phase.sh` + 4 more scripts; broad-form heading rules with fenced-code-block awareness for backfill insertion AND `## Test Spec Revisions` placement; backticked-token-required-for-MISSING regression guard against prose-token false-positive; parsed-state enrollment of backfill phases in coverage floor; frontmatter complete→active flip; +10 fixtures, +103 tests, `56df394`; first push CI failed because `git grep` found the supposedly-absent backticked token in the AC body itself once committed — fix excluded the plan file from the search via `:(exclude)<plan-rel>` pathspec + new `git_isolate_dir` test helper, second push CI green and auto-merged), #140 (Phase 6: 11 conformance checks in `tests/test-skill-conformance.sh` (one per WI 6.3 sub-bullet) covering frontmatter, tracking-marker, NOT-a-finding list, zero-findings-valid, coverage-floor, **orchestrator's judgment** memory anchor, broad-form checksum-boundary, broad-form backfill insertion, broad-form TSR placement, fenced-code-block-aware boundary, hardened jq-absence; worked example under `tests/fixtures/draft-tests/examples/`; mirror via `scripts/mirror-skill.sh draft-tests`; finalize-marker contract documented; +18 tests, `522cc9e`; verifier dispatched inline due to org agent-dispatch usage limit reached partway through, caveat documented in commit message and phase report). Tests 1213→1670 (+457 net new — 379 own across the plan + 78 absorbed from concurrent merges). Plan frontmatter `status: complete; completed: 2026-04-30`. **Phase F's DRAFT_TESTS_SKILL_PLAN entry is done; new `/draft-tests` skill shipped, sister to `/draft-plan`, scoped to test specs.** Two notable mid-run incidents both surfaced + fixed inline (skill-framework-repo discipline): Phase 2 stale Tier-1 hash defect caught by verifier (impl re-edited script after registering hash, never re-recorded), and Phase 5 verifier-found `set -u` no-op-branch bug in `append-backfill-phase.sh` masked by impl test's `2>/dev/null`.

---

## TL;DR — execution checklist

Status legend: `[x]` complete · `[ ]` pending · `[~]` in flight (PR open or plan being drafted)

#### Phase A — pre-flight (fixes the tools the plans use to land)

- [x] `/fix-issues 56` — `/commit` respects `execution.landing` (PR #74, merged 2026-04-27)
- [x] `/quickfix ← QF1` — slug-namespace `/draft-plan` review files (PR #79, merged 2026-04-28)
- [x] `/quickfix ← QF2` — orchestrator-judgment convergence fix (PR #82, merged 2026-04-28)
- [x] `/quickfix ← QF4` — `/refine-plan` positional-tail guidance + spaces-in-paths fix (PR #85, merged 2026-04-28)
- [x] `/run-plan plans/IMPROVE_STALENESS_DETECTION.md` — landed via PRs #90, #91, #92 (chunked finish auto, 2026-04-28)

#### Phase B — foundation

- [x] `/run-plan plans/SCRIPTS_INTO_SKILLS_PLAN.md` — landed via PRs #94 (refine-plan), #95, #96, #97, #98, #99, #100 (chunked finish auto, 2026-04-28)

#### Phase C — DEFAULT_PORT reconciliation (mandatory /refine-plan)

- [x] `/refine-plan plans/DEFAULT_PORT_CONFIG.md` — landed via PR #119 (a7d9656, 2026-04-28; absorbed Phase B inline-landing of WIs 1.1–1.3)
- [x] `/run-plan plans/DEFAULT_PORT_CONFIG.md` — landed via PR #125 (da108b4, 2026-04-29; chunked `finish auto` PR mode; all phases (2 + P1.A + 3 + 4 + 5); tests 1213→1347 (+14 own, +59/121 from concurrent merges); CHANGELOG corrected to track auto-backfill as future work, NOT shipped)

#### Phase D — Tier-2 plans (run sequentially to reduce mirror churn)

- [x] `/refine-plan plans/CONSUMER_STUB_CALLOUTS_PLAN.md` — landed via PR #105 (2026-04-28)
- [x] `/run-plan plans/CONSUMER_STUB_CALLOUTS_PLAN.md` — landed via PR #106 (2026-04-29; parallel session)
- [x] `/refine-plan plans/SKILL_FILE_DRIFT_FIX.md` — landed via PR #121 (2026-04-29; 2 rounds; 35+36 findings; refine-2 reframed away `__SNAKE__`/sed mechanism)
- [x] `/run-plan plans/SKILL_FILE_DRIFT_FIX.md` — landed via PR #122 (2026-04-29; PR-mode chunked finish auto; all 5 phases; tests 1213→1272 (+59); helper script + reference doc + drift-warn hook + deny-list + allowlist convention)

#### Phase E — /update-zskills source discovery (must wait for B+C+D)

- [x] `/quickfix ← QF3` — landed via PR #128 (2026-04-30T04:23 UTC, closes Issue #126: `/update-zskills` source-asset probe extension + stop-and-ask replacing silent auto-clone). QF3 was moved to Issue #126 on 2026-04-29 (per `QUEUED_QUICKFIXES.md:69`); PR #128 closed it via the `/fix-issues 123 126 93 89 110` sprint that also landed PR #127 (issue #123 test-clobber), PR #129 (issue #93 hook bug), PR #130 (issue #89 test gap).

#### Phase F — independent plans (any order, post-Phase B is safest)

For each item: `/refine-plan` first to absorb drift introduced by Phase B / C / D landings since the plan was authored, then `/run-plan`. Skip the refine step only if you've verified the plan has no touchpoints with what's landed since.

- [x] `/refine-plan plans/BLOCK_DIAGRAM_TRACKING_CATCHUP.md` — landed via PR #103 (2026-04-28)
- [x] `/run-plan plans/BLOCK_DIAGRAM_TRACKING_CATCHUP.md` — landed via PR #109 (2026-04-29; closes Issue #65)
- [x] `/refine-plan plans/DRAFT_TESTS_SKILL_PLAN.md` — landed via PR #120 (efd5c28, 2026-04-28; absorbed post-2026-04-24 ecosystem changes)
- [x] `/run-plan plans/DRAFT_TESTS_SKILL_PLAN.md` — landed via PRs #124, #135, #136, #137, #139, #140 (2026-04-29 → 2026-04-30). All 6 phases complete; +457 tests (1213 → 1670). New `/draft-tests` skill (sister of `/draft-plan`, scoped to test specs) with 12 Tier-1 scripts (`parse-plan.sh`, `detect-language.sh`, `insert-prerequisites.sh`, `append-tests-section.sh`, `draft-orchestrator.sh`, `coverage-floor-precheck.sh`, `convergence-check.sh`, `review-loop.sh`, `gap-detect.sh`, `append-backfill-phase.sh`, `insert-test-spec-revisions.sh`, `flip-frontmatter-status.sh`, `re-invocation-detect.sh`, `verify-completed-checksums.sh`), 40 fixtures, 11 conformance checks, worked example under `tests/fixtures/draft-tests/examples/`. Load-bearing invariants: orchestrator-judgment convergence (refiner self-call ignored), broad-form section boundary with fenced-code-block awareness, backticked-token-required-for-MISSING (prose-only ACs never trigger), backfill phases enrolled in coverage-floor pre-check via parsed-state, `## Test Spec Revisions` placed AFTER `## Drift Log` / `## Plan Review` (closes /refine-plan checksum-boundary cross-skill interaction). Phase 5 surfaced + fixed two real bugs (gap-detect's `git grep` finding the AC's own backticked token; `set -u` no-op-branch crash). Phase 6 verifier dispatched inline due to org agent-dispatch usage limit; caveat documented.
- [x] `/refine-plan plans/QUICKFIX_DO_TRIAGE_PLAN.md` — landed via PR #146 (1573479, 2026-05-01). Two stacked refine commits on `feat/quickfix-do-triage`: first commit `52079e7` was a single-agent run after a dispatch architectural mismatch (`/refine-plan` wrapped in `Agent` → subagent had no `Agent` tool → multi-agent loop collapsed; agent honestly disclosed via tool-list inspection). Second commit `ba2120f` re-ran via the `Skill` tool from a top-level session for proper multi-agent. Round 1: 25 substantive findings (13 reviewer + 12 DA in parallel) → 24 fixed + 1 justified-not-fixed. Round 2: 10 unique substantive findings (housekeeping drift from round-1 structural changes) → 10 fixed. Total 35 findings dispositioned, 0 unresolved. Memory anchor `feedback_multi_agent_skills_top_level.md` recorded the gotcha; Issue [#143](https://github.com/zeveck/zskills-dev/issues/143) proposes a preflight Agent-tool-required check on the five multi-agent skills (`refine-plan`, `draft-plan`, `draft-tests`, `research-and-plan`, `research-and-go`) so this fails loud at the next occurrence.
- [x] `/run-plan plans/QUICKFIX_DO_TRIAGE_PLAN.md` — landed via PRs #151, #152, #153, #154, #156 (2026-05-01). All 5 phases complete in a single `finish auto` autonomous run (~95 min wall-clock, recurring `*/1` cron, fresh-top-level turn per phase). Suite 1698 → 1722 (+24 cases: 10 quickfix + 13 do, with one Case-51 split). New behavior: triage gate + inline plan composition + fresh-agent plan review on `/quickfix` and `/do` (orthogonal to `/verify-changes`). For `/do`, triage and review run BEFORE cron registration (Phase 0 split into 0a/0b/0c) so a redirected invocation leaves no zombie cron. New flags: `--force` (bypass) and `--rounds N` (review depth, default 1, `0` skips with WARN). Reviewer enforces `OBSERVABLE-SIGNAL RULE` (>4 Acceptance bullets → auto-REVISE with "too many concepts; consider /draft-plan"). VERDICT regex (APPROVE bare; REVISE/REJECT need `--` separator) in dedicated `regex` fence (NOT `bash`) to dodge tests/test-quickfix.sh's `extract_full_flow`. WI 1.5.5 decline-path now two-arm: production model-layer decline (no marker) vs test-fixture bash-fallback (`reason: user-declined`). Round-1 DA3 quoted-description carve-out preserves user-prose `--force`/`--rounds` substrings inside quoted heads. Round-1 DA12 mirror precondition uses `git status --porcelain` (catches untracked orphans `git diff --quiet` misses). Round-2 R5/DA4 Phase 1.5 re-validation does NOT exit 2 on non-numeric `--rounds`. Follow-up [Issue #155](https://github.com/zeveck/zskills-dev/issues/155) filed for `/commit pr` (same triage + plan-review pattern). Plan frontmatter flipped to `status: complete`; recurring cron self-terminates via Step 0 Case 1.
- [x] `/draft-plan plans/ADAPTIVE_CRON_BACKOFF.md` — landed via PR #131 (2026-04-30; `/draft-plan` 2 rounds adversarial review; Mode A only; Mode B deferred to #134)
- [x] `/run-plan plans/ADAPTIVE_CRON_BACKOFF.md` — landed via PR #138 (2026-04-30T22:06 UTC, merge commit `3a49a36`; 4 phase commits `685d03c`/`6131105`/`ff24d74`/`88f414b`; plan `status: complete` `6cd561e`; Issue #110 closed via Phase 5b)
- [x] `/refine-plan plans/ZSKILLS_MONITOR_PLAN.md` — landed via PR #101 (2026-04-28)
- [x] `/run-plan plans/ZSKILLS_MONITOR_PLAN.md` — landed via PRs #102, #104, #107, #108, #111, #113, #115, #116, #117 + #118 bookkeeping (2026-04-28 → 2026-04-29). All 9 phases complete; +270 tests (943 → 1213). New `/work-on-plans` + `/zskills-dashboard` skills; HTTP server with drag-drop dashboard; `/plans rebuild` migrated to Python aggregator.
- [x] `/draft-plan plans/PR_LANDING_UNIFICATION.md` — extract canonical `gh pr create` + CI poll + fix-cycle + auto-merge pattern into a new `/land-pr` skill consumed by all 5 PR-creating skills. [PR #77](https://github.com/zeveck/zskills-dev/pull/77) merged 2026-04-28; plan now on main.
- [x] `/refine-plan plans/PR_LANDING_UNIFICATION.md` — landed via PR [#157](https://github.com/zeveck/zskills-dev/pull/157) (`6ddebcb`, 2026-05-01). 2-round /refine-plan against post-2026-04-27 ecosystem drift, dispatched via Skill tool from top-level for proper multi-agent. **Round 1 (R5):** 17 findings (5 reviewer preserved + 12 DA — reviewer's 12-finding output was lost to an orchestrator file-management error during consolidation, recovered top-5 from task-completion summary; round 2 caught what was missed). 16 fixed, 1 justified. Major fixes: all mirror WIs replaced `rm -rf .claude/skills/X` with `bash scripts/mirror-skill.sh` (PR #88 canonical helper would have hook-blocked the old recipes); WI 1.5 + new WI 3.5a consolidate `/land-pr`'s pr-monitor.sh with already-shipped `skills/commit/scripts/poll-ci.sh` (PR #142); WI 2.1 splice citation corrected (claimed sed at SKILL.md:1216-1217; actual is BASH_REMATCH at :1715-1745); Phase 5 substantially rewritten for /quickfix's new triage gate + plan-review baseline (PRs #151-156); ADAPTIVE_CRON_BACKOFF Mode A interaction documented; 2x duplicated "Load-bearing architectural decisions" subsection reconciled. **Round 2 (R6):** 15 findings (4 reviewer + 11 DA, 3 HIGH). 11 fixed, 4 justified. Critical: **all 3 HIGH DA findings REPRODUCED — Round 1 had verify-before-fix discipline failures** (DA2-1: refiner said grep verified 0 hits for `gh pr create` in `do/SKILL.md`; actually present at line 878 — fixed; DA2-3: Round 1 introduced a fictional `## Step 6` anchor in WI 2.2 conflated with `/commit/modes/pr.md`; real anchors are `# --- PR creation ---` at line 268 and `# --- Auto-merge ---` at line 587 in `run-plan/modes/pr.md` — fixed; DA2-2: Phase 6 tripwires would false-fail on prose mentions across 3+ files — rewritten to use start-of-line-anchored invocation patterns). Conformance assertion count corrected from ~13 to 24 (run-plan=9, commit=3, do=4, fix-issues=8). **Cumulative across 6 rounds (3 /draft-plan + 1 YAGNI + 2 ecosystem-drift):** 92 findings dispositioned, 63 fixed, 29 justified, 0 ignored. Plan grew 912→1011→1077 lines (+18%). Pre-land verifier said READY-TO-LAND with 1 NIT (Round History per-row Justified column sum off by +2 from cumulative). Post-merge CI green.
- [x] `/run-plan plans/PR_LANDING_UNIFICATION.md` — landed via PRs #159, #160, #161, #162, #163, #164, #166 (chunked `finish auto` PR-mode, single session 2026-05-01). All 7 phases (1A foundation → 1B validation → 2 `/run-plan` migration → 3 `/commit pr` + `/do pr` migration → 4 `/fix-issues pr` migration → 5 `/quickfix` migration → 6 drift-prevention conformance + canary). All 5 PR-landing call sites now dispatch `/land-pr`; 4 of 5 callers GAINED canonical fix-cycle (drift fix). New `/land-pr` skill (552-line SKILL.md + 4 scripts + 2 caller-facing references + 10-failure-mode catalog + caller-loop pattern). 8 cross-skill anchored-pattern conformance tripwires lock in the unification. New `plans/CANARY_LAND_PR.md` exercises unified flow with deliberate fix-cycle. Tests 1722 → 1801 (+79). Phase 2 was the bootstrap canary (Phase 3 used the new `/run-plan` code; Phase 3+ used /land-pr dispatch — verified-in-action across 5 successive land-pr invocations). One real bug caught and fixed during Phase 1A smoke (BRANCH_SLUG for `/tmp` paths). One CI flake observed (test-briefing-parity minute-boundary timing race, sister to issue #132 — non-blocking, single rerun passed). Manual-canary WIs 2.9/3.5/3.10/4.8/5.8/6.6 deferred per architectural constraint (subagents can't run multi-agent skills); the 5 successive land-pr-dispatch landings + Phase 6's CANARY_LAND_PR plan serve as de-facto end-to-end canaries.
- [x] `/refine-plan plans/SKILL_VERSIONING.md` — landed via PR [#174](https://github.com/zeveck/zskills-dev/pull/174) (squash `3e68b5e`, 2026-05-02). 2-round refine in worktree `/tmp/zskills-refine-skill-versioning`. Round 1: 38 findings (21 reviewer + 17 DA), 28 fixed + 9 justified + 1 partial. Round 2: 18 regression-focused findings, all fixed — caught 1 regression Round 1 introduced (`json-set-string-field.sh` awk rewrite produced invalid JSON in update + insert paths; fixed). Plan grew 1339→1815 lines. Cumulative across all 5 refine + draft rounds: 56 dispositioned in this refine pass; 93 across all draft + refine history.
- [x] `/run-plan plans/SKILL_VERSIONING.md` — landed via PR [#175](https://github.com/zeveck/zskills-dev/pull/175) (squash `f0ef441`, 2026-05-02). Date+hash hybrid versioning system shipped: per-skill `metadata.version: "YYYY.MM.DD+HHHHHH"` (date + 6-char SHA-256 hash of canonical projection). 3-point enforcement chain wired: (1) edit-time warn via `hooks/warn-config-drift.sh` Branch 3 (PostToolUse, non-blocking), (2) commit-time hard stop via `scripts/skill-version-stage-check.sh` invoked from `/commit` Phase 5 step 2.5, (3) CI gate via `tests/test-skill-conformance.sh` (3 new sections: cleanliness, version frontmatter, mirror parity). Helper scripts added: `frontmatter-get.sh`, `frontmatter-set.sh`, `skill-content-hash.sh`. `/update-zskills` UI extended with delta-aware version reporting (Phase 5a/5b). **Two enforcement gaps surfaced during the run**, filed as issues + queued in Phase F: **#176** (Monitor anti-pattern recurrence — verifier dispatches reflexively reach for `Bash(run_in_background:true)` + `Monitor`/`BashOutput` polling; PR #148's prose warning failed mechanically across all 7 phase verifiers; orchestrator did inline verification across 5 of 7 phases) and **#180** (verifier-skipped silent pass — orchestrator logs a one-line note and proceeds instead of invoking Failure Protocol). Both motivated `VERIFIER_AGENT_FIX` (Plan A) below.
- [x] `/draft-plan plans/VERIFIER_AGENT_FIX.md` — landed via PR [#181](https://github.com/zeveck/zskills-dev/pull/181) (squash `144c85d`, 2026-05-02). Plan A of two structural-defense plans drafted post-PR-#175. **Closes #176 + #180** when `/run-plan`-executed. Three-layer fix: (1) Claude Code custom subagent at `.claude/agents/verifier.md` with `tools:` allowlist excluding `Monitor`/`BashOutput`/`Agent`, (2) frontmatter `PreToolUse` hook on Bash rejecting `run_in_background: true` via key-boundary-anchored bash regex, (3) `/run-plan` Phase 3 Failure Protocol clause that STOPs on stalled-string match (last-N-lines anchor, 7 behavioral phrases) or sub-200-byte verifier response. Separate `commit-reviewer.md` agent for `/commit`'s read-only phase (word-boundary regex catches `&&`/env-prefix/subshell/pipe bypasses). 5 dispatch sites migrated to explicit `subagent_type:` parameters: `/run-plan` Phase 3, `/commit` Phase 5 step 3, `/fix-issues` per-issue, `/do` Phase 3, `/verify-changes` self-dispatch. Drafted via `/draft-plan` 2 adversarial rounds: Round 1 = 17 closed; Round 2 = 9-of-10 closed (N9 whitelist-finite-recall deferred via filed-issue work item). Independent ship-readiness review: SHIP. **Notable Round-2 verified defects:** `/agents reload` is fictional in Claude Code 2.1.126 (`claude -p '/agents reload'` returns "isn't available"); `--agents` CLI flag takes inline JSON not file paths (Phase 1.1 canary spec rewritten as canonical two-script flow).
- [x] `/run-plan plans/VERIFIER_AGENT_FIX.md` — landed via PR [#189](https://github.com/zeveck/zskills-dev/pull/189) (squash `5db8283`, MERGED 2026-05-03). All 6 phases ✅ Done via chunked `finish auto pr` (recurring `*/1` cron, fresh-top-level turn per phase, 4 hours wall-clock total). Tests 2071/2071 PASS at every phase boundary. Closes #176 + #180. **D'' rework absorbed in Phase 1**: original L1+L2 architecture (allowlist exclusion + bg-rejection PreToolUse hook + commit-reviewer.md) replaced with D'' (Layer 0: `inject-bash-timeout.sh` PreToolUse hook auto-extends Bash timeout to 600000ms via `updatedInput`; Layer 3: `verify-response-validate.sh` script piped after every verifier dispatch — 7-phrase stalled-string whitelist + 200-byte minimum). Verifier.md kept full tools allowlist; Layer 0 prevents the trigger so structural restriction unnecessary. Phase 1 manual canary PASSED 2026-05-03 against Claude Code 2.1.126 (subagent auto-discovery from `.claude/agents/` confirmed). 5 dispatch sites migrated to explicit `subagent_type: "verifier"` + Layer 3 invocation pipe + STOP on exit 1: `/run-plan` Phase 3 (Phase 2 commit `636eccb`), `/commit` + `/fix-issues` + `/do` + `/verify-changes` (Phase 3 ordered commits `d6690d0`, `285bc1d`, `cf2fc56`, `5919ea9`). Phase 4 added 2 canaries (tools-allowlist + timeout-injection + Layer 3 script with 11 sub-cases A-G; +18 tests). Phase 5 extended `/update-zskills` Step C with hook list +2 + agent-copy block + install summary (5-file commit incl. cross-plan jq-invariant fix in `tests/test-update-zskills-version-surface.sh`). Phase 6 docs: CLAUDE.md verifier rule refreshed to D'' mechanics; CHANGELOG entry; Anthropic upstream issue draft at `plans/reports/VERIFIER_AGENT_FIX-anthropic-issue-draft.md` (filing deferred to user — no vendor-channel auth from this environment). Single round-1 verifier fix-cycle on Phase 5 (jq disclaimer collision; refined invariant). All 5 stale-tracking-marker cleanup invariants pass. **Process win**: chunked finish-auto via recurring `*/1` cron + Step 0 idempotent re-entry worked end-to-end across 4 fresh top-level turns; verifier subagent dispatched correctly after manual install of `verifier.md` to main's `.claude/agents/` (Claude Code re-scans `.claude/agents/` on each Agent dispatch attempt — contradicts initial "session-start only" assumption).
- [x] `/draft-plan plans/SKILL_VERSION_PRETOOLUSE_HOOK.md` — landed via PR [#182](https://github.com/zeveck/zskills-dev/pull/182) (squash `3f28847`, 2026-05-02). Plan B of two. **Closes the lock-step gap PR #175 left open**: bare `git commit` from terminal bypasses `/commit` step 2.5's stage-check; CI is the only mechanical safety net for what lands on a feature branch. Fix is a PreToolUse Bash hook (`hooks/block-stale-skill-version.sh`) that intercepts `git commit` invocations and DENIES on stale `metadata.version` hashes. Decisions D1-D5 made and justified: flat hook (no `.template`; cosmetic per research F4); commit-only gating (push gating dropped — cached-diff is empty at push time); KEEP /commit step 2.5 (defense-in-depth); pure-bash JSON escape with `LC_ALL=C` + named escapes + POSIX `[[:cntrl:]]` strip; mandatory consumer helper-script install (research F1 verified zero copy lines for the 4 stage-check helpers in current `/update-zskills`). Drafted via `/draft-plan` 2 adversarial rounds: Round 1 = 17 closed (D6 collapses into R1); Round 2 = 10 closed. Independent ship-readiness review: SHIP. **Notable Round-2 verified defects:** Round 1 regex still bypassed `git --no-pager commit`/`--git-dir=`/`-P` etc. — Round 2 pivoted to **tokenize-then-walk** `is_git_commit` shell function (more robust than any regex); bash range strip pattern `[$'\x00'-$'\x1f']` empirically only matched 0x1F not the full range — replaced with POSIX `[[:cntrl:]]`; Phase 4.2 cited wrong Step (Step B is rules-render; Step C is hook/script-copy) — corrected. **Plan B Phase 5.6 follow-up redirect** (post-merge): PR [#184](https://github.com/zeveck/zskills-dev/pull/184) edits the merged plan to redirect the block-unsafe-project.sh:404 over-match follow-up from "file an issue" to "recommend `/draft-plan plans/BLOCK_UNSAFE_HARDENING.md`" — see Phase G backlog row + 2026-05-02 drift entry below for rationale.
- [x] `/refine-plan plans/SKILL_VERSION_PRETOOLUSE_HOOK.md` — landed via PR [#192](https://github.com/zeveck/zskills-dev/pull/192) (squash `c68997f`, MERGED 2026-05-06). 2 cumulative refinement rounds; 26 deduped findings hard-fixed. Round 1 (annotation refresh — orchestrator-imposed scope after Plan A landed and shifted Step C anchors): 5 stale line-number references + 1 sentence added to Phase 5.2 CLAUDE.md note + Drift Log + Plan Review section. Round 2 (NORMAL scope after user removed scope cap): verifier-hook composition resolved EMPIRICALLY via Anthropic Code docs (composition CONFIRMED — frontmatter PreToolUse hooks fire ALONGSIDE project settings.json hooks, NOT REPLACE; Phase 5.2 paragraph rewritten with citation + verifier-side recovery + orchestrator-side recovery + hook-chain composition note); CHANGELOG format mismatch fix (date-only H2 + `### Added —` H3 to match Plan A's landed shape; date-collision spec); Phase 4.2 moved Step C → Step D (canonical script-install home, line 1090; AC11 inverted; AC10 mtime-only reword; new AC12); Phase 3.1c explainer scoped to "Installing 3 PreToolUse Bash safety hooks" (NOT "6" — explainer is PreToolUse-Bash-scoped, not all-hooks-scoped); Phase 3.1 split into 3.1a/3.1b/3.1c with order-independence note; Phase 5.6 D3 strengthened with two DA-observed live reproducers; 8 medium/low fixes. Plan grew 633 → 765 lines.
- [x] `/run-plan plans/SKILL_VERSION_PRETOOLUSE_HOOK.md` — landed via PR [#193](https://github.com/zeveck/zskills-dev/pull/193) (squash `0e911a6`, MERGED 2026-05-06). All 5 phases ✅ Done via chunked `finish auto pr` (recurring `*/1` cron, fresh-top-level turn per phase). Tests **2071 → 2111** (+40 cases: +27 from Phase 2 unit suite + +13 from Phase 4 sandbox integration). Closes lock-step gap PR #175 left open. **Phase highlights:** P1 reference doc (D1-D5 + R1/R2/R3 manual recipes); P2 `hooks/block-stale-skill-version.sh` (tokenize-then-walk `is_git_commit` per Round 2 N1 + pure-bash `json_escape` LC_ALL=C + POSIX `[[:cntrl:]]` per D4 + `bash -c` carve-out documented); P3 `.claude/settings.json` registration + `update-zskills` SKILL.md canonical table 5→6 + Step C copy bullet + explainer "Installing 3 PreToolUse Bash safety hooks" + mirror; P4 `scripts/install-helpers-into.sh` shared driver (Step D placement post-refinement) + 13-case sandbox integration test (verifier caught + fixed missing `chmod +x` on driver); P5 CHANGELOG `## 2026-05-06` H2 + `### Added —` H3 + CLAUDE.md PreToolUse-backstop note (composition-citation per Anthropic docs + verifier-side recovery via Edit+Bash allowlist + orchestrator-side recovery) + canary RECIPE doc + post-merge followups doc. **Process win:** chunked finish-auto via recurring `*/1` cron worked across 5 fresh top-level turns; verifier subagent's Layer 0 timeout-injection hook + Layer 3 response-validate composed correctly with project hooks (composition empirically confirmed during refinement); Plan A's structural defense delivered as designed.
- [x] `/draft-plan plans/ZSKILLS_PATH_CONFIG.md` — landed via PR [#198](https://github.com/zeveck/zskills-dev/pull/198) (squash `af45aa5`, MERGED 2026-05-07). Two docs landed together: `ZSKILLS_PATH_CONFIG_PROPOSAL.md` (pre-plan design brief — two-tier curation split, separate `output.plans_dir`/`output.issues_dir` config keys, `.zskills/audit/` fixed for forensic + regenerated indexes incl. `PLAN_INDEX.md`, `var/` → flat `.zskills/dev-server.{pid,log}`) + `ZSKILLS_PATH_CONFIG.md` (3,343-line implementation plan; 9 phases). 3 rounds adversarial review (47→40→25 findings; P0 trajectory 6→5→1→0); orchestrator-caught one verified-false-premise refiner Justification (decimal phase numbering claim) + amended via fixup commit (renamed Phase 1.5 → Phase 1b). Mid-amendment also reclassified `PLAN_INDEX.md` from Tier 1 to Tier 2 (regenerated-index shape matches `PLAN_REPORT.md`/`VERIFICATION_REPORT.md`); 3 verifier P1s addressed (step 4b verification block, Case 1 fixture, proposal/plan tier-classification consistency). Cleanup branch `cleanup/remove-zimulink-viewer-refs` (PR #196) was the prerequisite for Phase 4 and merged 2026-05-06 (same day as drafting), so Phase 4.0's gate is already satisfied on main. Docs-only — no skill/hook/script/test changes; implementation lands via `/run-plan` below.
- [x] `/refine-plan plans/ZSKILLS_PATH_CONFIG.md` — landed via PR [#206](https://github.com/zeveck/zskills-dev/pull/206) (squash `a81607b`, MERGED 2026-05-07; 2-round refine absorbing post-2026-05-06 ecosystem drift; 22 + 11 fixes). See drift log entry.
- [x] `/run-plan plans/ZSKILLS_PATH_CONFIG.md` — landed via PR [#211](https://github.com/zeveck/zskills-dev/pull/211) (squash `f0c2a13`, MERGED 2026-05-10). All 9 phases (1, 1b, 2a, 2b, 3, 4, 5a, 5b, 6) bundled in one squash (per-phase commits `5b9f150`/`6082e1f`/`6c2dc50`/`6b9552f`/`59aeff7`/`99ad5df`/`12042ae`/`0e12b4e`/`9479e09` on `feat/zskills-path-config`). 206 files changed (+7441/−756). Phase 6 self-migration moved 105 files (`plans/` → `docs/plans/`, forensic → `.zskills/audit/`, `var/dev` → `.zskills/dev-server`). Tests 2747 → 2805. Plan frontmatter `status: complete; completed: 2026-05-08`. Post-migration smoke landed via PR [#213](https://github.com/zeveck/zskills-dev/pull/213) (CANARY1_HAPPY); registry-orphan class-of-bug surfaced + fixed structurally via PR [#214](https://github.com/zeveck/zskills-dev/pull/214) (Case 6d git-reachability gate). See drift log entry.

#### Phase G — deferred / backlog

- `plans/GITLAB_SUPPORT_DRAFT_PLAN_PROMPTS.md` *(reference; not executable yet — Issue #67)*
- [x] `/draft-plan plans/BLOCK_UNSAFE_HARDENING.md` — landed via PR [#192](https://github.com/zeveck/zskills-dev/pull/192) (squash `c68997f`, MERGED 2026-05-06; bundled with Plan B refinement). Plan C of three structural-defense plans. Triggered by Plan B Phase 5.6 redirect (PR #184, 2026-05-02 drift entry) + 5 in-session reproducers of the over-match bug class hit during Plan B's refinement + this draft's review (the bug is currently active and bites real workflows, not just review tooling). Drafted via `/draft-plan` 2 adversarial rounds; converged at round 2 with 0 critical/high/medium findings remaining. Generalized `is_git_subcommand` helper at new `hooks/_lib/git-tokenwalk.sh` source-of-truth (byte-identical inlined into both hooks at install time, drift-gated by Phase 5.4); covers 6 sites in `block-unsafe-project.sh` (404 commit + 411 commit-verify + 540/546 cherry-pick + 616/719 push) and selected sites in `block-unsafe-generic.sh` (106 git-restore + 120 git-checkout-double-dash + 246 git-add-dot — destructive verbs that benefit from tokenize-then-walk). `rm`/`find`/`rsync`/`xargs`/`fuser-mk` destructive sites kept-bare per D5 carve-out lock to preserve pipeline coverage like `xargs rm` and `pgrep | xargs kill`. 8-row D5 carve-out table locked by NEGATIVE unit-test cases (XCC30/31/32/34, XKL11/12). Phase 6 handles Plan B coordination: dispatch `/refine-plan plans/SKILL_VERSION_PRETOOLUSE_HOOK.md` if Plan B is active; edit Plan B's hook directly with citation if Plan B is complete. Out-of-scope (recorded for follow-up): PUSH_ARGS pre-existing carve-out + newline-separated command bypass. Plan: 976 lines. Independent ship-readiness review: SHIP. **Notable Round-2 verified defects**: PR3 demoted to UNTRACED (orchestrator's `gh issue list --search 'OR'` reproducer empirically didn't trip any current regex when isolated; honest documentation as "untraced reference" beats false class-pinned AC); line 120 (`git checkout --`) reinstated to migration scope after refiner's session live-block reproduced the over-match class; read-ra quote-blindness accepted as documented carve-out (D5) rather than implementing hand-rolled quote-aware tokenizer that would re-introduce regex-fragility class.
- [x] `/run-plan plans/BLOCK_UNSAFE_HARDENING.md` — landed via PR [#195](https://github.com/zeveck/zskills-dev/pull/195) (squash `ad22145`, MERGED 2026-05-06). All 6 phases ✅ Done via chunked `finish auto pr`. Tests **2111 → 2700** (+589 across the plan: 127 unit cases for source-of-truth helpers in P2, then class-pinned matrices in P5 with 144+192+24 cases, plus integration cases). Generalized `is_git_subcommand` + `is_destruct_command` helpers consolidated in `hooks/_lib/git-tokenwalk.sh` (single source-of-truth, byte-identical inlined into 3 hook consumers via Phase 5.4 drift gate). P3 migrated `block-unsafe-project.sh.template` 6 sites; P4 migrated `block-unsafe-generic.sh` selected sites; P6 consolidated Plan B's `block-stale-skill-version.sh` to consume the source-of-truth (per D6). 8-row D5 carve-out table (`rm`/`find`/`rsync`/`xargs`/`fuser-mk` kept-bare to preserve pipeline coverage like `xargs rm` and `pgrep | xargs kill`) locked by NEGATIVE unit-test cases. Per-phase commits: `2c0c4f1` (P1) / `57706e9` (P2) / `561a73c` (P3) / `d566512` (P4) / `e18d1e8` (P5) / `38791d3` (P6). **Plan C complete; the structural-defense triple (Plans A/B/C) is now landed end-to-end.** Follow-up PR #197 (2026-05-07) extended the drift gate from 4 → 7 byte-identity assertions to cover the chain wrappers (`is_git_subcommand_in_chain` + `is_destruct_command_in_chain`); see drift log entry.

#### Open issues — disposition

Issues filed but not yet routed to a phase. Default to running clear-and-doable items as a parallel `/fix-issues` batch; route design-class items to `/draft-plan` first.

- [x] **Issue [#132](https://github.com/zeveck/zskills-dev/issues/132)** — closed 2026-04-30T21:07 ET via PR [#141](https://github.com/zeveck/zskills-dev/pull/141), merge commit `43a2071`. Fix landed in two commits: initial `hourCycle: 'h23'` with `hour12: false` retained (failed CI on older Node), then fix-up `22f1b98` dropped `hour12: false` (per MDN: when both are present, hour12 takes precedence and hourCycle is ignored) AND added `parts.hour === '24'` → `'00'` belt-and-suspenders. +5 midnight-ET regression assertions in `tests/test-briefing-parity.sh` Phase 4.
- [x] **Issue [#133](https://github.com/zeveck/zskills-dev/issues/133)** — closed 2026-04-30T21:09 ET via PR [#142](https://github.com/zeveck/zskills-dev/pull/142), merge commit `b77d589`. Implemented **option 1 + option 2** per the issue body's recommendation (option 3 = tracking marker + hook explicitly declined as overkill). New `skills/commit/scripts/poll-ci.sh` (57 lines, executable); `modes/pr.md` Step 6 prose updated with `Past failure (2026-04-30):` blockquote and replaces inline block with `bash poll-ci.sh "$PR_NUMBER"` invocation. +2 conformance assertions in `tests/test-skill-conformance.sh`.
- [x] **Issue [#143](https://github.com/zeveck/zskills-dev/issues/143)** — closed 2026-05-01T22:50 ET via PR [#169](https://github.com/zeveck/zskills-dev/pull/169), merge commit `a71a68d`. Added `## Preflight — top-level dispatch required` block to all 5 multi-agent skills (`/refine-plan`, `/draft-plan`, `/draft-tests`, `/research-and-plan`, `/research-and-go`) and their `.claude/skills/` mirrors. `/research-and-go` uses the variant wording ("Skill-loads /research-and-plan and /run-plan, which internally dispatch..."); other 4 use the direct wording. +5 conformance assertions in `tests/test-skill-conformance.sh` (one per skill, anchored on the heading literal `## Preflight — top-level dispatch required`). Each preflight references `feedback_multi_agent_skills_top_level.md`. Tests 1801→1807 (+5 conformance asserts + 1 unrelated baseline drift).
- [x] **Issue [#150](https://github.com/zeveck/zskills-dev/issues/150)** — closed 2026-05-01T22:53 ET via PR [#170](https://github.com/zeveck/zskills-dev/pull/170), merge commit `4c92fbd`. Implemented option 1 from the issue body: `tests/test_zskills_monitor_collect.sh` Phase 4 AC `worktree-portable` now materializes its own temp worktree via `git worktree add --detach` when invoked from primary repo, runs `collect_snapshot()` in both contexts, compares stable subset, tears down with `git worktree remove --force`. Trap-based cleanup ensures teardown on early exit. The case used to SKIP-as-pass under primary-repo invocation (CI's `actions/checkout@v4` always produces a primary clone); now runs unconditionally. +1 PASS from primary-repo invocation contexts, no leaked tempdirs verified.
- [x] **Issue [#165](https://github.com/zeveck/zskills-dev/issues/165)** — closed 2026-05-01T22:55 ET via PR [#171](https://github.com/zeveck/zskills-dev/pull/171), merge commit `12a405b`. Added `dashboard.work_on_plans_trigger` (string, default `""`) to `config/zskills-config.schema.json`. Added Step 3.6 idempotent backfill to `skills/update-zskills/SKILL.md` (parallel to existing 3.5 commit.co_author backfill); greenfield template now includes the dashboard block. Removed `ensure_dashboard_config_block()` (44 lines) and its startup call site from `skills/zskills-dashboard/scripts/zskills_monitor/server.py` — server is now read-only on `.claude/zskills-config.json`. Documented the read-only boundary in `skills/zskills-dashboard/SKILL.md`. Flipped `tests/test_zskills_monitor_server.sh` assertion to lock the new contract (server does NOT mutate `zskills-config.json` on launch).
- [x] **Issue [#176](https://github.com/zeveck/zskills-dev/issues/176)** — closed via Plan A `/run-plan` PR [#189](https://github.com/zeveck/zskills-dev/pull/189) (squash `5db8283`, MERGED 2026-05-03). Three-layer structural defense delivered: Layer 0 (`inject-bash-timeout.sh` PreToolUse hook auto-extends Bash timeout to 600000ms via `updatedInput`) prevents the trigger that drove agents to reach for `Monitor`/`BashOutput`; Layer 3 (`verify-response-validate.sh` 7-phrase stalled-string whitelist + 200-byte minimum) catches anything that slips through; verifier custom subagent at `.claude/agents/verifier.md`. 5 dispatch sites migrated to explicit `subagent_type: "verifier"`.
- [x] **Issue [#180](https://github.com/zeveck/zskills-dev/issues/180)** — closed via same PR [#189](https://github.com/zeveck/zskills-dev/pull/189). The verifier-skipped silent pass is now caught by Layer 3's response validator (exit 1 on stalled-string match → STOP per Failure Protocol).
- [x] **Issue [#134](https://github.com/zeveck/zskills-dev/issues/134)** — Closed `wontfix` 2026-05-11T19:36Z with structured rationale comment ([issue-comment-4424236296](https://github.com/zeveck/zskills-dev/issues/134#issuecomment-4424236296)). Conclusion: the Mode B failure mode (cron fires queue up behind paused orchestrator, drain on resume) **does not exist in the Anthropic Code harness** because the harness is retry-on-next-idle per `skills/run-plan/references/finish-mode.md:103,125` (not queue+drain). A 5-hour orchestrator pause produces ONE deferred fire on resume, not N queued fires; Mode A's existing Step 0 prelude handles it. Option (c) "accept Step-0-only" is correct **by absence-of-trigger, not absence-of-incident**. Re-evaluation trigger keyed on harness-change (not missing-incident counting): re-open iff Anthropic Code harness publishes queue+drain semantics, OR third-party runner with queue+drain semantics consumes zskills. Plan file `docs/plans/ADAPTIVE_CRON_BACKOFF_MODE_B.md` (219 lines, originally landed via PR #223) deleted via PR [#230](https://github.com/zeveck/zskills-dev/pull/230) (squash `cebb194`, MERGED 2026-05-11T19:37:30Z). No skill, hook, script, or test changes — close-comment IS the deliverable.
- [x] **Issue [#188](https://github.com/zeveck/zskills-dev/issues/188)** — closed 2026-05-07 via PR [#208](https://github.com/zeveck/zskills-dev/pull/208) (squash `f8ba2c9`, bundled with #203 + #205). Replaced conditional `@{u}`-based push with always-explicit `git push -u origin "$BRANCH"` + post-push `git ls-remote` verification (mismatch → exit 12 with `CALL_ERROR_FILE`). +3 cases in `tests/test-land-pr-scripts.sh` + 1 conformance assertion blocks regression to bare `git push`.
- [x] **Issue [#185](https://github.com/zeveck/zskills-dev/issues/185)** — Closed 2026-05-02T20:01 ET via [PR #187](https://github.com/zeveck/zskills-dev/pull/187) (squash `cf8e44e`). Agent-dispatch discipline rule added to CLAUDE_TEMPLATE.md + zskills CLAUDE.md (never call `gh pr create`/`gh pr merge --auto` directly — dispatch `/land-pr` via Skill tool or use one of its 5 callers). `skills/land-pr/SKILL.md` line 4 description rewritten to clarify agent-via-Skill-tool IS the supported path; restriction is only against interactive human slash-command invocation. `metadata.version` bumped to `2026.05.02+e41e24` per skill-versioning enforcement. 9th conformance tripwire added (4 PASS lines locking the rule literals against silent prose drift in both files). Tests 2029→2033 (+4 from tripwire). Bundled with #186 in single PR (same files); fix agent grouped both per `/fix-issues` skill convention. Pre-merge orchestrator-process failure surfaced + cleanly recovered: `pr-push-and-create.sh` exit 13 on first attempt because the script's `git push` lost a race / the gh credential warnings interfered with stderr/stdout; manual `git push -u origin fix/issue-185` succeeded; re-ran /land-pr per its idempotency contract → push no-op, create succeeded, monitor PASS, merge MERGED. Worth filing as a follow-up issue (TBD) for `pr-push-and-create.sh` push-failure detection.
- [x] **Issue [#186](https://github.com/zeveck/zskills-dev/issues/186)** — Closed 2026-05-02T20:01 ET via [PR #187](https://github.com/zeveck/zskills-dev/pull/187) (same squash `cf8e44e` as #185 — bundled). Memory-anchor meta-rule added to CLAUDE_TEMPLATE.md + zskills CLAUDE.md: memory anchors are agent-local notes, not propagating fixes for skill gaps; real fixes go in CLAUDE_TEMPLATE.md (consumer-wide) / skill SKILL.md prose (skill-specific) / helper scripts (only when no judgment OR script returns enough info for agent to judge) / skill decomposition (when structural). Same conformance tripwire as #185 covers this rule's literal too.
- [x] **Issue [#177](https://github.com/zeveck/zskills-dev/issues/177)** — closed 2026-05-07T07:42 ET via PR [#199](https://github.com/zeveck/zskills-dev/pull/199) (squash `6a0f5b1`). New `skills/run-plan/scripts/pr-preflight.sh` helper accepts `--exclude-pr` to filter the pipeline's own PR; SKILL.md prose updated to cite the helper. 9-case unit suite added.
- [x] **Issue [#178](https://github.com/zeveck/zskills-dev/issues/178)** — closed 2026-05-07T07:45 ET via PR [#200](https://github.com/zeveck/zskills-dev/pull/200) (squash `704e16a`, bundled with #179). New `scripts/skill-version-compare.sh` implements `date >= old AND (date > old OR hash != old)`; Comparator subsection added to `references/skill-versioning.md`. 18-case unit suite.
- [x] **Issue [#179](https://github.com/zeveck/zskills-dev/issues/179)** — closed 2026-05-07T07:45 ET via PR [#200](https://github.com/zeveck/zskills-dev/pull/200) (same squash as #178). `tests/test-skill-conformance.sh` failure message now cites `<!-- allow-hardcoded: -->` marker with placement guidance; `\`\`\`text` fence formally documented as non-exec exemption (Appendix C). Audit confirmed zero bare-fence body literals.
- [x] **Issue [#183](https://github.com/zeveck/zskills-dev/issues/183)** — closed 2026-05-07 via PR [#207](https://github.com/zeveck/zskills-dev/pull/207) (squash `0332696`). Regex in `skills/zskills-dashboard/scripts/zskills_monitor/collect.py` `[—-]` → `[—–:\-]` (now accepts em-dash, en-dash, colon, hyphen). Prose consistency in `skills/plans/SKILL.md` + `skills/draft-plan/SKILL.md` documents canonical separator + acceptance set. New fixture exercises all 4 separator variants; +2 assertions in `test_zskills_monitor_collect.sh`. metadata.version bumped on 3 affected skills.
- [~] **Issue [#191](https://github.com/zeveck/zskills-dev/issues/191)** — Plan DRAFTED via `/fix-issues plan 134 191 auto` (2026-05-11), landed via PR [#223](https://github.com/zeveck/zskills-dev/pull/223) as `docs/plans/RUN_PLAN_FINISH_AUTO_REUSE_SEMANTICS.md`. Plan scope (after multiple rounds of user-correction during drafting): cherry-pick mode in `finish` / `finish auto` switches from per-phase worktree + per-phase landing to plan-scoped worktree + accumulate-and-land-once-at-end (matching PR mode, which already does this correctly per SKILL.md:1198). Single-phase invocations (`/run-plan plan.md 3`) stay per-phase by design. 3 phases (~242 lines), READY for `/run-plan`. Issue auto-closed by PR #223's lenient keyword match — REOPENED manually because only the plan draft landed, not the code change.
- [x] **Issue [#194](https://github.com/zeveck/zskills-dev/issues/194)** — closed 2026-05-07T07:48 ET via PR [#201](https://github.com/zeveck/zskills-dev/pull/201) (squash `e8d637e`). STOP message now appends `(SKILL.md not staged — git add it)` when `on_disk_ver != staged_ver && on_disk_ver != head_ver` (case b: bumped on disk but not in staged blob). Variable-name discrepancy from issue body resolved by using actual signal. 2 new test cases.
- [x] **Issue [#202](https://github.com/zeveck/zskills-dev/issues/202)** — closed 2026-05-07 via PR [#209](https://github.com/zeveck/zskills-dev/pull/209) (squash `a4c6ba2`). `mktemp` + EXIT trap in `tests/test-hooks.sh` post-run-invariants block; 9 prior shared-path usages all migrated. Verified hermetic-under-load during sibling-worktree concurrent verification of the 2026-05-07 11:45 sprint. Single-file change.
- [x] **Issue [#203](https://github.com/zeveck/zskills-dev/issues/203)** — closed 2026-05-07 via PR [#208](https://github.com/zeveck/zskills-dev/pull/208) (bundled with #188 + #205). One-char ref change in `skills/land-pr/SKILL.md` Step 8 COMMITS_LIST: `$BASE_BRANCH` → `origin/$BASE_BRANCH` so `.landed` `commits:` list isn't over-populated by stale-local-main during sequential-PR sprints. Test gap documented (faithful reproducer would require multi-squash sequential-PR fixture, disproportionate for one-char fix).
- [x] **Issue [#205](https://github.com/zeveck/zskills-dev/issues/205)** — closed 2026-05-07 via PR [#208](https://github.com/zeveck/zskills-dev/pull/208) (bundled with #188 + #203). Step 8 marker write was silent-skip when `--worktree-path` omitted (gap that left 3 unmarked worktrees this session prior to filing). Auto-detect from cwd via `git rev-parse --show-toplevel` + `git worktree list` membership; treats `--worktree-path` as override, not gate. New `tests/test-land-pr-worktree-detect.sh` (8 cases: 4 anchor checks + 4 behavioral A/B/C/D).
- [x] **Issue [#134](https://github.com/zeveck/zskills-dev/issues/134)** — Closed `wontfix` 2026-05-11T19:36Z with structured rationale comment ([issue-comment-4424236296](https://github.com/zeveck/zskills-dev/issues/134#issuecomment-4424236296)). Conclusion: the Mode B failure mode (cron fires queue up behind paused orchestrator, drain on resume) **does not exist in the Anthropic Code harness** because the harness is retry-on-next-idle per `skills/run-plan/references/finish-mode.md:103,125` (not queue+drain). A 5-hour orchestrator pause produces ONE deferred fire on resume, not N queued fires; Mode A's existing Step 0 prelude handles it. Option (c) "accept Step-0-only" is correct **by absence-of-trigger, not absence-of-incident**. Re-evaluation trigger keyed on harness-change (not missing-incident counting): re-open iff Anthropic Code harness publishes queue+drain semantics, OR third-party runner with queue+drain semantics consumes zskills. Plan file `docs/plans/ADAPTIVE_CRON_BACKOFF_MODE_B.md` (219 lines, originally landed via PR #223) deleted via PR [#230](https://github.com/zeveck/zskills-dev/pull/230) (squash `cebb194`, MERGED 2026-05-11T19:37:30Z). No skill, hook, script, or test changes — close-comment IS the deliverable.
- [x] **Issue [#212](https://github.com/zeveck/zskills-dev/issues/212)** — closed 2026-05-11T13:17Z via PR [#221](https://github.com/zeveck/zskills-dev/pull/221) (squash `45fc622`). Extracts `/run-plan` Phase 4 PR-body progress sync to `skills/run-plan/scripts/sync-pr-body-progress.sh` (190 lines pure bash). 7 unit cases + 9 conformance assertions. Tier-1 registered. PR went through one rebase cycle (originally based on PR #218's untrack commit ef3e36f; rebased onto post-#222 main, then again onto post-#223 main).
- [x] **Issue [#215](https://github.com/zeveck/zskills-dev/issues/215)** — closed 2026-05-11T04:25Z via PR [#222](https://github.com/zeveck/zskills-dev/pull/222) (squash `cd43835`, bundled with #216). Extracts `/plans rebuild` index-rendering loop to `skills/plans/scripts/render-index.py` (~600 lines pure stdlib Python). Canary-precedence enforced at line 62-64's FIRST branch in `section_for()`. 7 cases / 26 sub-assertions + 5 new conformance asserts. AC-7 follow-up commit `00e329e` on the same branch landed the regenerate-if-absent fix for `tests/test_plans_rebuild_uses_collect.sh`, unblocking CI for this PR + the still-failing PRs #218/#221.
- [x] **Issue [#216](https://github.com/zeveck/zskills-dev/issues/216)** — closed 2026-05-11 manually via comment pointing to PR [#222](https://github.com/zeveck/zskills-dev/pull/222) (same squash `cd43835` as #215; GitHub auto-close failed to detect "Closes #215 AND #216" because of the "AND" — the bundled close needs `Closes #215, closes #216` syntax to fire). PR #222 added Mode: Show source-staleness auto-rebuild + removed `/draft-plan` + `/research-and-plan`'s incremental PLAN_INDEX.md mutation (verified via grep returning 0 hits).
- [ ] **Issue [#217](https://github.com/zeveck/zskills-dev/issues/217)** — relocate plan execution reports (`plan-{slug}.md`) and project-wide work-trail aggregates (`FIX_REPORT.md`, `SPRINT_REPORT.md`, `PLAN_REPORT.md`) out of `.zskills/audit/` into `docs/plans/reports/` + `docs/reports/`. **Route: `/draft-plan` first** when triggered (explicit "not immediately" guidance in issue body — wait for: someone trips on the audit-dir tracking inconsistency, next minor release scoped, or a user asks "where do I find the report for plan X"). Architectural follow-up to PR #211 closing the only remaining shape-asymmetry: 52 files in `.zskills/audit/` are tracked despite the dir being gitignored (because `git mv` preserved tracking through the migration), so the dir is currently a hybrid of work-trail + forensic exhaust. Estimated 3-4 phases. Multiple integration points (helper, mover, multiple writers, multiple readers, cross-ref rewrite, conformance) — design surface benefits from adversarial review.
- [x] **Issue [#220](https://github.com/zeveck/zskills-dev/issues/220)** — filed by user 2026-05-10T21:45Z during the /fix-issues 212/215/216 sprint when AC-7 (`tests/test_plans_rebuild_uses_collect.sh`) failed on fresh-checkout CI for any branch based on PR #218's untrack commit. Closed via PR #222's follow-up commit `00e329e` (the AC-7 regenerates-if-absent fix); resolution comment posted on the issue. Same-session file-and-close pattern.
- [~] **Issue [#224](https://github.com/zeveck/zskills-dev/issues/224)** — structural enforcement: PreToolUse hook blocks bypassed `/land-pr` (combined tracker + tool-boundary patterns; #185 follow-up). Filed 2026-05-11 after the same-session `/land-pr` bypass demonstrated three times (PRs #218/#221/#223) despite #185's prose-only rule + 9th conformance tripwire. **Pattern 2 partially landed for `/run-plan` ↔ `/land-pr` via PR [#219](https://github.com/zeveck/zskills-dev/pull/219)** (MERGED squash `2bdbeda` at 2026-05-11T19:12:16Z; required 2 rebases onto post-#221/#222/#223 then post-#227 main). Scope-update comment on #224 ([#issuecomment-4423411465](https://github.com/zeveck/zskills-dev/issues/224#issuecomment-4423411465)) classifies remaining work as: (1) extend tracker pattern to the 4 other callers (`/commit pr`, `/do pr`, `/fix-issues pr`, `/quickfix`); (2) **Pattern 1 — tool-boundary block** via new dedicated `hooks/block-bypassed-land-pr.sh` denying `gh pr (create|merge --auto)` at PreToolUse when no `fulfilled.land-pr.*` exists; (3) reconcile fulfilled-marker lifecycle — recommend Option B (Step 1 `status: started` + Step 9 `status: complete`, mirroring `/verify-changes`) over #219's Step 8b row-6 conjunction, with the row-6 gate migrated into Step 9's content update. Effort estimate post-#219: ~4-5 phases. **Route: `/draft-plan plans/BYPASSED_LAND_PR_HARDENING.md` first** — multi-skill integration (4 remaining callers + new dedicated hook + `hooks/_lib/git-tokenwalk.sh` extension for `is_gh_pr_subcommand` + conformance + canary + lifecycle migration on top of #219's plumbing). Acknowledged cost: `! gh pr create` from Claude REPL is blocked (terminal access remains). Acknowledged residual: agent-deliberate-bypass-and-rationalize remains uncatchable at the hook layer.
- [ ] **Issue [#225](https://github.com/zeveck/zskills-dev/issues/225)** — Block agent direct commits to main + assert local-main-not-ahead-of-origin in `create-worktree.sh`. Filed 2026-05-11T18:01Z after the ef3e36f trace (an agent commit on local main propagated into PR #219's branch when `create-worktree.sh`'s ff-merge no-op'd on AHEAD state, masking the failure as "pre-existing AC-7"). **Scope-update comment posted 2026-05-11 ([#issuecomment-4424362086](https://github.com/zeveck/zskills-dev/issues/225#issuecomment-4424362086)):** Layer 1 (hook block on `git commit` while on main when `main_protected`) is **already implemented** at `hooks/block-unsafe-project.sh.template:476` — landed via PR #195 (Plan C — BLOCK_UNSAFE_HARDENING, 2026-05-06), refined via PR #197. The ef3e36f commit likely happened via `! git commit` in the REPL (terminal channel bypasses PreToolUse). Remaining scope: **Layer 2 only** — `create-worktree.sh` adds `AHEAD_COUNT=$(git rev-list --count origin/main..main)` ahead-check after the existing `git fetch + ff-merge` block, exits with error if non-zero. **Route: `/fix-issues`** when triggered — ~10 lines + 2-3 unit cases, single file, no `/draft-plan` needed (pattern is identical to Layer 1).
- [ ] **Issue [#226](https://github.com/zeveck/zskills-dev/issues/226)** — `/draft-plan` should auto-create worktree + auto-commit plan when project is in PR mode + main_protected. Filed 2026-05-11T18:07Z. Sister concern to #225: in a project with `execution.landing: pr` + `main_protected: true`, standalone `/draft-plan` invocations leave the plan file uncommitted in main's working tree, which the user can't even commit due to the protection. User reports having to repeatedly instruct agents "use /draft-plan on a worktree." **Route: `/fix-issues`** when triggered — two coordinated additions: (A) new Phase 0 auto-worktree gate when `execution.landing == pr` AND `main_protected == true` AND caller is standalone AND orchestrator is NOT already in a worktree; uses `create-worktree.sh --prefix draftplan --pipeline-id draft-plan.<slug>`. (B) Phase 6 auto-commit step inside the worktree (`git add` + `git commit -m "docs(plans): draft <slug>"`). NOT FIXED — confirmed no Phase 0 or commit step in current SKILL.md.
- [ ] **Issue [#228](https://github.com/zeveck/zskills-dev/issues/228)** — `clear-tracking.sh` preserves only `fulfilled.run-plan.*`, deleting `fulfilled.verify-changes.*`, `fulfilled.draft-plan.*`, `fulfilled.refine-plan.*` despite those being canonical completion records per CLAUDE.md / `/run-plan` Phase 5b framing. Filed 2026-05-11T19:24Z. Two coordinated concerns: (A) clear-tracking scope too narrow (line 49 explicitly preserves only `fulfilled.run-plan.*`); (B) several landing/completing skills don't write fulfilled markers AT ALL (`/commit`, `/quickfix`, `/do`, `/land-pr`, `/fix-issues`) — so even widening scope A leaves the dashboard's activity feed blind to ~50% of completion events. Asymmetric scope was a deliberate `a9ad570` (2026-04-18) decision and has been stable through 2 subsequent commits without any prior issue. NOT FIXED. **Route: `/draft-plan` first** — multi-skill integration (5 callers need fulfilled-marker writes) + clear-tracking scope widening + post-clear assertion update + dashboard activity-feed coverage parity. Touches the same callers `/draft-plan plans/BYPASSED_LAND_PR_HARDENING.md` (#224) would touch — consider whether the two plans should bundle.
- [ ] **Issue [#229](https://github.com/zeveck/zskills-dev/issues/229)** — Z Skills Monitor rename → Z Skills Dashboard + split single-page view into tabs (OVERVIEW / PLANS / ISSUES / ETC). Filed 2026-05-11T19:25Z. Pure presentation refactor — no behavior/content/polling/data-source changes; just tab navigation primitive + panel rerouting + header rename. Issue body explicitly notes "best practice... varies on whether to use horizontal tabs, vertical nav, or hybrid" and asks for UI design agent review. NOT FIXED — confirmed `<title>Z Skills Monitor</title>` and `<h1>Z Skills Monitor</h1>` in `skills/zskills-dashboard/scripts/zskills_monitor/static/index.html:6,11`; no `role="tab"` / `tablist` markup. **Route: `/draft-plan`** when triggered — UI design surface benefits from adversarial review on tab structure + accessibility (WCAG tab patterns, keyboard navigation, focus management).
- [ ] **Issue [#67](https://github.com/zeveck/zskills-dev/issues/67)** — GitLab support. **Route: stays in Phase G.** Hard prerequisites met (SCRIPTS_INTO_SKILLS, SKILL_FILE_DRIFT_FIX, CONSUMER_STUB_CALLOUTS all complete) but still needs a real GitLab project to test against; revisit when that's in hand.

**Suggested next step (2026-05-11 late-evening update — after `/fix-issues sync` pass):** Two parallel routes, both productive:

1. **Small mechanical fix first: `/fix-issues 1 225 auto pr`** — closes #225 Layer 2 (single ~10-line addition to `create-worktree.sh` plus 2-3 unit cases). Smallest unit of remaining work; closes the local-main-ahead-of-origin propagation path that caused #219's "pre-existing AC-7" confusion. No `/draft-plan` overhead — pattern is identical to Layer 1 which shipped without a plan.

2. **Plan execution: `/run-plan docs/plans/RUN_PLAN_FINISH_AUTO_REUSE_SEMANTICS.md finish auto pr`** — closes #191. 3 phases, actual code change to cherry-pick mode (`finish`/`finish auto` switches from per-phase worktree+land to plan-scoped worktree+accumulate-and-land-once-at-end). Touches SKILL.md cherry-pick worktree-creation block + landing-flow gating + conformance test 146 + CANARY7 ACs.

Can run in parallel — #225 Layer 2 touches `create-worktree.sh`; #191 touches `skills/run-plan/SKILL.md` + `modes/cherry-pick.md`. No file overlap.

**After both land:** consider bundling **#224 + #228** under a single `/draft-plan plans/SKILL_LANDING_TRACKER_PARITY.md` — both touch the same 5 caller skills (`/commit`, `/quickfix`, `/do`, `/land-pr`, `/fix-issues`), both involve fulfilled-marker write-coverage parity, and #228 widens `clear-tracking.sh`'s preservation set to whatever fulfilled markers #224 starts writing. Drafting them together avoids two passes through the same files.

**Remaining backlog after that:** **#226** (/draft-plan auto-worktree in PR mode — `/fix-issues` when triggered), **#229** (dashboard rename + tabs — `/draft-plan` for UI design surface), **#217** (relocate audit-dir reports — `/draft-plan` when triggered), **#67** (GitLab — Phase G, needs real project).

**Hygiene to clean up at convenience:** `.landed` markers for the 2026-05-11 session's earlier worktrees (`/tmp/zskills-fix-issue-212`, `/tmp/zskills-fix-issue-215`, `/tmp/zskills-docs-session-plans-and-sprint`) — three need manual markers; `/tmp/zskills-do-requires-land-pr-tracker` (#219) and `/tmp/zskills-close-134` (#230) have markers auto-written by /land-pr Step 8.

Earlier sprint **`/fix-issues 177 178 179 194 auto`** ran 2026-05-07 06:28→07:48 ET (PRs #199/#200/#201, all green-CI auto-merge first-try) — see drift log entry.

Earlier parallel batch **`/fix-issues 143 150 165 auto`** ran 2026-05-01 22:45→22:55 ET (PRs #169/#170/#171, all auto-merged on green CI first try). See drift log entry. The structural-defense triple (Plans A/B/C — VERIFIER_AGENT_FIX + SKILL_VERSION_PRETOOLUSE_HOOK + BLOCK_UNSAFE_HARDENING) landed end-to-end across PRs #189, #193, #195 + follow-up #197.

---

## Why this order — the load-bearing constraints

### 1. Pre-flight quickfixes & issues come before every plan

These four items all touch the **tooling that plans use to land**: `/commit`, `/draft-plan`, `/refine-plan`. Landing them first means every subsequent `/run-plan`, `/refine-plan`, or `/quickfix` runs against fixed orchestration code instead of carrying the bugs forward.

| Item | Touches | Why first |
|---|---|---|
| Issue #56 | `skills/commit/SKILL.md` (config-read for `execution.landing`) | `/commit` is invoked at the end of every plan landing; today it ignores the `pr` mode the plans assume. |
| QF1 | `skills/draft-plan/SKILL.md:L126` | Concurrent `/draft-plan` runs collide on `/tmp/draft-plan-review-round-N.md`. |
| QF2 | `skills/draft-plan/SKILL.md` + `skills/refine-plan/SKILL.md` (4 sites total) | Convergence is an orchestrator judgment, not the refiner's self-call; the buggy behavior currently rubber-stamps "CONVERGED" mid-loop. |
| QF4 | `skills/refine-plan/SKILL.md` (Arguments + Phase 2) | Adds positional-tail guidance arg so `/refine-plan plans/X.md anti-deferral focus` works. Useful for the Phase C reconciliation below. |

These are mutually independent — order within Phase A doesn't matter — but **all four must land before Phase B** so the Phase-B/C/D plans don't trip them.

(Issue #58 was originally listed here; closed via PR #73 — see Drift log above.)

### 2. SCRIPTS_INTO_SKILLS_PLAN is the foundation gate

Two plans hard-block on it via in-plan staleness gates:

- `CONSUMER_STUB_CALLOUTS_PLAN.md` — Phase 1 staleness gate halts `/run-plan` if `SCRIPTS_INTO_SKILLS_PLAN` status ≠ `complete`.
- `SKILL_FILE_DRIFT_FIX.md` — Phase 0 has the same gate.

A third plan **overlaps** but doesn't gate:

- `DEFAULT_PORT_CONFIG.md` — duplicates `SCRIPTS_INTO_SKILLS Phase 3a` work items 3a.4c.i/ii/iii (schema field, this-repo config, greenfield template). PLAN_INDEX explicitly says: *"reconcile via /refine-plan after one of the two lands."*

Running `SCRIPTS_INTO_SKILLS_PLAN.md` first is the canonical path because it's the more structural change and the two follow-up plans (CONSUMER_STUB_CALLOUTS, SKILL_FILE_DRIFT_FIX) anchor on its Tier-1 layout.

### 3. DEFAULT_PORT_CONFIG needs `/refine-plan` mid-stream

After Phase B lands, `DEFAULT_PORT_CONFIG.md` Phase 1 work items 1.1–1.3 are already done by `SCRIPTS_INTO_SKILLS_PLAN` Phase 3a. The remaining novel work is **WI 1.4 (backfill logic in `/update-zskills` for existing configs)** plus Phases 2–5.

**Mandatory step**: `/refine-plan plans/DEFAULT_PORT_CONFIG.md` between Phase B and Phase C. The refine pass strips WIs 1.1–1.3 as done-elsewhere and validates the surviving backfill scope. Skipping this means `/run-plan` will spend a phase double-writing the schema field and possibly fail acceptance criteria that test for "field added by this plan."

If you'd rather run `DEFAULT_PORT_CONFIG.md` first, the symmetric reconciliation works (`/refine-plan plans/SCRIPTS_INTO_SKILLS_PLAN.md` after) — but `SCRIPTS_INTO_SKILLS_PLAN` is bigger and more structural, so foundation-first is cleaner.

### 4. CONSUMER_STUB_CALLOUTS and SKILL_FILE_DRIFT_FIX should run sequentially, not in parallel

Both Tier-2 plans add files to `.claude/skills/update-zskills/`:

- `CONSUMER_STUB_CALLOUTS` adds `zskills-stub-lib.sh`.
- `SKILL_FILE_DRIFT_FIX` adds `zskills-resolve-config.sh`.

Both use `rm -rf .claude/skills/update-zskills && cp -a skills/update-zskills/ ...` discipline. Running them in parallel risks the second wiping the first's mirror copy. Sequential execution avoids that with no extra coordination.

There's no hard ordering between the two — pick whichever you'd rather land first. CONSUMER_STUB → DRIFT_FIX is the order matching the Tier-2 priority in PLAN_INDEX.

### 5. QF3 is *deferred by design*

`QUEUED_QUICKFIXES.md` Prompt 3 explicitly states:

> queued instead because (a) low urgency — doesn't break installs, just produces silent re-clone when a non-`/tmp` clone exists; (b) `skills/update-zskills/SKILL.md` is going to churn from active plans (DEFAULT_PORT_CONFIG, SCRIPTS_INTO_SKILLS_PLAN, SKILL_FILE_DRIFT_FIX) — running this now would create a refine-after-rebase loop.

Run QF3 only **after Phases B + C + D-DriftFix** complete. It's anchored on the section name and 4-tier probe rather than line numbers, so it survives the file churn.

### 6. Issue #65 → run the existing plan, not `/fix-issues`

`BLOCK_DIAGRAM_TRACKING_CATCHUP.md` (3 phases, in PR #70) was authored specifically for Issue #65. The plan goes beyond a raw fix and adds:

- Phase 1: core marker migration (19 sites in `add-block` + `add-example`).
- Phase 2: lint guard + 2 new canary test cases.
- Phase 3: framework-coverage CI guard preventing recurrence.

**Use `/run-plan plans/BLOCK_DIAGRAM_TRACKING_CATCHUP.md`** — `/fix-issues 65` would land Phase 1 only and skip the enforcement that prevents the issue from re-emerging on the next batch migration.

The plan is fully independent of the Phase B/C/D chain (block-diagram is its own skill family, no shared files), so it can run any time after Phase A. Doing it after Phase B is mildly safer — `IMPROVE_STALENESS_DETECTION` and the canonical-config helpers will already be in place.

### 7. Independent plans in Phase F

These four plans share no files, no schema fields, and no skill mirrors with each other or with Phase B–E:

- `BLOCK_DIAGRAM_TRACKING_CATCHUP.md` — block-diagram only.
- `DRAFT_TESTS_SKILL_PLAN.md` — new `skills/draft-tests/` skill.
- `QUICKFIX_DO_TRIAGE_PLAN.md` — `skills/quickfix/` + `skills/do/` triage gates.
- `ZSKILLS_MONITOR_PLAN.md` — new `/zskills-dashboard` + `/work-on-plans` skills, adds its own config block.

Run in any order. Two soft considerations:

- **Run after QF2 lands** (which it already has by Phase A). DRAFT_TESTS and QUICKFIX_DO_TRIAGE both reference adversarial-review patterns; if either was authored against the old "refiner self-declares" model, consider an optional `/refine-plan` first. Not strictly required — the convergence behavior change is a mechanical fix that doesn't invalidate plan content.
- **ZSKILLS_MONITOR_PLAN refresh is in PR #70** (`/zskills-monitor` → `/zskills-dashboard`). Run it as-is.

### 8. GITLAB_SUPPORT_DRAFT_PLAN_PROMPTS — defer indefinitely

PLAN_INDEX flags it as `Reference (deferred)`. It's planning prompts for *future* work, not an executable plan. Hard prerequisites: `SCRIPTS_INTO_SKILLS_PLAN`, `SKILL_FILE_DRIFT_FIX`, `CONSUMER_STUB_CALLOUTS_PLAN` all complete + a real GitLab project to test against. Don't `/run-plan` this; revisit when both conditions are met.

---

## Refine-plan checklist

> **Note (2026-05-02):** This section is effectively deprecated as a tracking surface — all refines since Phase D have been tracked in the main TL;DR checklist above. Kept here for historical record.

Required:

- [x] `/refine-plan plans/DEFAULT_PORT_CONFIG.md` — landed via PR #119 (2026-04-28).

Optional / consider:

- [x] `/refine-plan plans/DRAFT_TESTS_SKILL_PLAN.md` — landed via PR #120 (2026-04-28).
- [x] `/refine-plan plans/QUICKFIX_DO_TRIAGE_PLAN.md` — landed via PR #146 (2026-05-01).

Not needed:

- `BLOCK_DIAGRAM_TRACKING_CATCHUP.md`, `ZSKILLS_MONITOR_PLAN.md`, `IMPROVE_STALENESS_DETECTION.md`, `CONSUMER_STUB_CALLOUTS_PLAN.md`, `SKILL_FILE_DRIFT_FIX.md`, `SCRIPTS_INTO_SKILLS_PLAN.md` — all freshly authored or refreshed in PR #70 against current state; no stale assumptions to refine.

---

## Conflict matrix (load-bearing files only)

| File / surface | Touched by |
|---|---|
| `skills/commit/SKILL.md` | Issue #56 |
| `skills/draft-plan/SKILL.md` | QF1, QF2 |
| `skills/refine-plan/SKILL.md` | QF2, QF4 |
| `skills/update-zskills/SKILL.md` | SCRIPTS_INTO_SKILLS_PLAN, SKILL_FILE_DRIFT_FIX, DEFAULT_PORT_CONFIG, CONSUMER_STUB_CALLOUTS_PLAN, QF3 |
| `.claude/skills/update-zskills/` (mirror) | CONSUMER_STUB_CALLOUTS_PLAN, SKILL_FILE_DRIFT_FIX (run sequentially) |
| `.claude/zskills-config.json` schema → `dev_server.default_port` | SCRIPTS_INTO_SKILLS_PLAN Phase 3a, DEFAULT_PORT_CONFIG Phase 1 (overlap → refine) |
| `block-diagram/add-block/SKILL.md` + `add-example/SKILL.md` | BLOCK_DIAGRAM_TRACKING_CATCHUP.md (Issue #65) — isolated |
| `skills/draft-tests/` (shipped 2026-04-30) | DRAFT_TESTS_SKILL_PLAN.md — done |
| `skills/quickfix/` + `skills/do/` | QUICKFIX_DO_TRIAGE_PLAN.md — isolated |
| `/zskills-dashboard` + `/work-on-plans` (new) | ZSKILLS_MONITOR_PLAN.md — isolated |

---

## Source documents

- PR #70: <https://github.com/zeveck/zskills-dev/pull/70>
- Issues: ~~[#56](https://github.com/zeveck/zskills-dev/issues/56)~~ (closed by PR #74) · ~~[#58](https://github.com/zeveck/zskills-dev/issues/58)~~ (closed by PR #73) · [#65](https://github.com/zeveck/zskills-dev/issues/65)
- [PR #75](https://github.com/zeveck/zskills-dev/pull/75) — `/fix-issues` PR-mode gating fix, merged 2026-04-27
- `QUEUED_QUICKFIXES.md` (repo root) — full prompts for QF1–QF4
- `plans/PLAN_INDEX.md` — auto-generated dependency notes
