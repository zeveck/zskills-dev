# Canary Test Files

PR mode pipeline canary — these files exercise the multi-phase PR workflow.

- `hello.sh` — Phase 1: hello-world function with self-test
- `math.sh` — Phase 2: arithmetic functions with self-test

Created by `/run-plan plans/CANARY_PR_TEST.md finish auto pr`
