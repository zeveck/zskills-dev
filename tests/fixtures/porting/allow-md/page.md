# Ported page — HTML-form allow-marker fixture

Ported prose with one retained Claude-lane mention, covered by the marker on
the line immediately above it (removing the marker un-suppresses the hit —
the suite pins both directions).

<!-- codex-port-allow: arguments reason: documented invocation-text example retained per recipe:arguments-invocation-text -->
The legacy invocation text arrived in $ARGUMENTS; Codex passes the mention text instead.
