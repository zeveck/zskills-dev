# Scoped-gate fixture — dirty subtree

This page still carries the Claude-lane invocation text: execute the
procedure with $ARGUMENTS set to the trailing text. An unscoped gate over
the fixture tree fails on this line; a gate scoped to `clean/` never sees
it.
