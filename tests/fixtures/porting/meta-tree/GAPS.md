# GAPS — port-instance record (meta-record exemption fixture)

Freeform gap prose naturally names the Claude constructs at issue — that is
this file's job, and the scanner exempts it by exact root-relative path.

- The guide gave no ruling on whether an AskUserQuestion confirm inside a
  destructive step becomes a plain-prose question or a hard refusal.
- The slice agent could not decide how the cron-fire fallback interacts with
  CronCreate re-arm text after the runner replaced scheduling.
- $ARGUMENTS semantics for multi-token trailing text were ambiguous in the
  invocation-text recipe.
