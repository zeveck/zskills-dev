# PORTING-NOTES — port-instance record (meta-record exemption fixture)

The CronCreate re-arm text in the run-plan port was rewritten to the
runner's handoff marker; the Skill tool dispatch sites became
read-SKILL.md-and-execute-inline per the guide recipe.
